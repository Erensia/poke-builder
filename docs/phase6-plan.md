# Phase 6 기획 — MVP 완성도 다듬기 (데이터·기능 정비, 버그 수정, UI 개선)

> 이어서 읽기: [Phase 5 기획](phase5-plan.md)
> Phase 5에서 특성/기술 전투 로직을 대거 완성하면서 사용자가 Phase 6으로 명시적으로 이월한 3개 항목(헤롱헤롱 성별 데이터, 샘플파티/샘플 저장, 로그 UI 정비)을 그대로 가져왔고, 여기에 더해 Phase 6 전체의 성격을 "새 대규모 기능보다는 기존 데이터·기능 재점검, 작은 버그 수정, UI 개선 — MVP 완성도를 다듬는 페이즈"로 사용자가 확정했다(2026-08-26).

## 0. 이 문서의 성격

Phase 1~5는 매번 "새 엔진 인프라를 만드는" 확장 페이즈였다. Phase 6은 방향이 다르다 — 새 기능보다는 지금까지 쌓인 것들을 다시 살펴보고 다듬는 데 집중한다. 이 문서는 두 부분으로 나뉜다:

- **§1**: Phase 5에서 이미 "이건 Phase 6에서 한다"고 확정해둔 3개 항목. 배경·요구사항은 phase5-plan.md에 이미 적혀있던 걸 그대로 옮겨왔다.
- **§2**: 사용자가 이번에 확정한 "다듬기" 방향에 따른 정비 후보. Phase 5 §5(발견된 버그)와 같은 성격의 living 섹션이라, 작업하면서 계속 추가·정리한다 — 지금 적힌 건 착수 시점에 이미 알려진 것들뿐이고 전수 조사 결과가 아니다.

## 1. Phase 5에서 이월된 확정 작업

### 1-1. ~~헤롱헤롱(매혹) — 포켓몬 성별 데이터 모델링~~ — **완료(2026-08-26)**

Phase 4.5 §5-1부터 미뤄져 온 "상대 기술 선택을 제약하는 변화기 4종(도발·사슬묶기·앙코르·헤롱헤롱)" 중 유일하게 남은 항목이었다. 도발·사슬묶기·앙코르 3개는 Phase 5에서 완료됐다(phase5-plan.md §2 참고).

- ~~**막힌 지점**: 포켓몬 성별 데이터가 이 프로젝트에 아예 없다.~~
- ~~**필요한 결정**: 종족별 성비를 배틀마다 어떻게 확정할지, 무성별 종은 어떻게 표시할지.~~
- ~~**구현**: 확률부 행동불능 로직, 헤롱헤롱바디는 `hitTrigger` 인프라 재사용.~~

**정책 결정(사용자 확인)**: 배틀마다 랜덤 배정하지 않고, 파티 슬롯 편집 화면에서 사용자가 성별을 직접 고르는 방식으로 확정 — "종족 성비를 정확한 비율로 시뮬레이션"보다 "사용자가 원하는 성별을 그대로 쓸 수 있게" 하는 쪽이 작업량도 더 적고 UX도 낫다는 판단. 미지정이면 수컷을 기본값으로 취급한다.

**구현 내용**:
- `types/pokemon.ts`에 `PokemonGenderCategory`("both"/"male-only"/"female-only"/"genderless")와 `Pokemon.genderCategory` 신설. 로스터 42종 전수 조사(Bulbapedia 성비 기준) 결과 39종이 "both", **메타그로스·타부자고**만 무성별, **캥카**만 암컷 전용 — male-only는 이 로스터에 해당 종이 없었다.
- `types/party.ts`의 `PartySlot`에 `gender?: PokemonGender`(사용자가 고른 값, "both" 종에서만 의미 있음) 신설.
- `lib/pokemonForm.ts`에 `getEffectiveGender(pokemon, slot)` 신설 — `getEffectiveAbilityId`와 같은 패턴으로, 종족 카테고리가 단일성별/무성별이면 슬롯 값과 무관하게 그 값으로 고정하고, "both"일 때만 슬롯 선택(미지정 시 수컷)을 따른다.
- `types/status.ts`의 `VolatileCondition`에 `"attract"` 신설 — ingrain/leechSeed와 같은 "배틀 끝까지 유지"형(1v1이라 교체로 해제될 일이 없음). `resolveAction`에서 걸려있는 동안 매 행동 판정마다 50% 확률로 행동불능(`ATTRACT_ACTION_BLOCK_CHANCE`, `volatileConditions.ts`).
- 헤롱헤롱(기술): `moves.json`에 `inflictsVolatile: [{volatile:"attract", target:"opponent"}]` 추가, 기존 `inflictsVolatile` 처리 루프에 "이성 관계 확인(무성별/동성이면 무산) + 이미 헤롱헤롱 상태면 재시전 실패" 게이트만 얹었다 — 새 메커니즘 불필요, 도발과 같은 재사용 패턴.
- 헤롱헤롱바디(특성): `types/ability.ts`의 `AbilityHitTrigger`에 `inflictsVolatileOnAttacker`/`requiresOppositeGender` 신설, `abilities.json`에 `{on:"contact", chance:30, inflictsVolatileOnAttacker:"attract", requiresOppositeGender:true}` 배선 — 기존 정전기/불꽃몸과 완전히 같은 `hitTrigger` 인프라를 그대로 재사용해서 예상대로 로직 추가 없이 끝났다.
- UI: `PartySlotCard.tsx`/`BattleSetupCard.tsx`에 "성별" 메타 pip 추가(종족 카테고리가 "both"일 때만 노출, 클릭 시 수컷/암컷 토글) — `useParty.ts`/`useBattleSetup.ts`에 `toggleGender` 신설.
- 대전 로그: `blockedReason === "attract"` 문구, 헤롱헤롱바디 발동 시 "OO의 헤롱헤롱바디! ○○은(는) 헤롱헤롱 상태가 되었다!" 전용 줄 추가.
- **스모크 테스트**(`tsx`로 배틀 로직 직접 실행): 이성끼리 헤롱헤롱 성공, 동성/무성별 대상은 무산, 무성별 종(메타그로스)은 `gender: null`, 암컷 전용 종(캥카)은 슬롯에 수컷을 지정해도 항상 `female`로 고정, 헤롱헤롱 걸린 상태에서 400회 시행 중 202회(50.5%) 행동불능, 헤롱헤롱바디 400회 시행 중 120회(30.0%) 발동 — 전부 기대 확률대로 확인.
- **범위 밖으로 남긴 것**: `멘탈허브`(헤롱헤롱·도발·연속불가·기술봉인·회복봉인·앙코르 상태 회복) 아이템은 이번에도 로직을 안 붙였다 — 도발/사슬묶기/앙코르도 이 아이템으로 치료하는 로직이 원래 없어서(설명 텍스트만 존재), 헤롱헤롱만 먼저 붙이면 오히려 일관성이 깨진다고 판단. 5개 상태 전부를 한 번에 붙이는 건 별도 작업으로 §2-3 후보에 남겨둔다.

### 1-2. ~~샘플파티 저장 기능~~ — **완료(2026-08-27)**

파티(6슬롯) 여러 개를 이름 붙여 저장/불러오기.

- `types/party.ts`에 `Party`(이미 스캐폴딩돼 있던 타입에 `savedAt` 필드 추가)를 프리셋 스키마로 재사용. `storage.ts`에 "현재 작업 중인 파티"(기존 `champions-party-sim.party.v1`, 그대로 유지)와 완전히 별도인 `champions-party-sim.party-presets.v1` 신설.
- `hooks/usePartyPresets.ts` 신설(`savePreset`/`renamePreset`/`deletePreset`, `useParty`와 같은 자동저장 `useEffect` 패턴) + `useParty.ts`에 `loadSlots(nextSlots)` 신설(6슬롯 통째 교체).
- UI: `PartyBoard.tsx` 헤더에 "저장된 파티" 버튼 → `PartyPresetsModal.tsx`(이름 입력해 현재 파티 저장, 목록에서 불러오기/이름변경/삭제). 불러오기는 `window.confirm`으로 "현재 편성 중인 파티를 덮어씁니다" 확인을 거친다(데이터 유실 방지).
- 스모크 테스트(브라우저로 직접 조작): 리자몽 2마리로 파티를 구성 → "테스트 파티"로 저장(2/6마리 표시 확인) → 슬롯 하나를 비워 1/6으로 만든 뒤 저장된 파티에서 불러오기 → 확인 통과 후 2/6으로 정확히 복원되는 것 확인.

### 1-3. ~~샘플 저장 기능~~ — **완료(2026-08-27)**

포켓몬 빌드 1개(파티 슬롯 하나)를 이름 붙여 저장/재사용.

- `types/party.ts`에 `SlotPreset`(`{ id, name, slot: PartySlot, savedAt }`) 신설 — 1-2의 파티 프리셋과 저장 단위가 달라 별개 목록. `storage.ts`에 `champions-party-sim.slot-presets.v1` 신설.
- `hooks/useSlotPresets.ts` 신설(1-2와 동일한 CRUD 패턴) + `useParty.ts`에 `loadSlot(index, slot)` 신설(슬롯 하나만 통째 교체).
- UI: 채워진 슬롯 카드(`PartySlotCard.tsx`)에 💾 아이콘 버튼("이 빌드를 샘플로 저장", `window.prompt`로 이름 입력) 추가. 빈 슬롯 카드에는 저장된 샘플이 하나라도 있을 때만 "저장된 샘플에서 불러오기" 링크가 나타나 `SlotPresetsModal.tsx`(불러오기/이름변경/삭제)를 연다 — 불러오기는 슬롯이 이미 채워져 있을 때만 덮어쓰기 확인을 거친다.
- **범위 밖으로 남긴 것**: 저장된 샘플의 포켓몬을 나중에 로스터에서 빼면 참조가 깨질 수 있는데, 이 프로젝트는 로스터 삭제 기능 자체가 없어서 당장은 발생할 수 없는 시나리오 — 향후 로스터 정리 기능이 생기면 참고.
- 스모크 테스트(브라우저): 리자몽 슬롯을 "테스트 리자몽"으로 저장 → 다른 빈 슬롯에서 "저장된 샘플에서 불러오기" → 불러오기 클릭 시 그 슬롯에 리자몽이 정확히 채워지는 것 확인(빈 슬롯이라 확인 없이 즉시 반영).

**추가 확장(사용자 요청, 2026-08-27): 대전 로그·매치업 페이지에서도 샘플 불러오기**

파티 편성 화면 전용으로 끝내지 않고, 다른 두 화면에도 샘플 기능을 연결했다. `useSlotPresets`(저장소)는 세 화면이 전부 공유 — 한 화면에서 저장한 샘플이 나머지 두 화면에도 그대로 보인다.

- **대전 로그 페이지**(`BattleSetupCard.tsx`/`useBattleSetup.ts`): 슬롯 타입이 파티 편성 화면과 완전히 같은 `PartySlot`이라 저장/불러오기 둘 다 그대로 이식 — `useBattleSetupSlot`에 `loadSlot(slot)` 추가.
- **매치업 페이지**(`MatchupSlotCard.tsx`/`useMatchup.ts`): 슬롯 타입이 다르다(`MatchupSlot`은 기술을 4개가 아니라 1개(`moveId`)만 갖고, 랭크 상태(`stages`)를 별도로 가짐) — 사용자 확인 결과 (1) **불러오기만 지원, 저장 기능 자체는 뺀다** (2) 불러올 때 **기술 배치는 가져오지 않는다**(포켓몬을 바꾸면 항상 그러듯 기술/랭크는 초기화)로 확정. `useMatchupSlot`에 `loadFromPartySlot(partySlot)` 신설 — `pokemonId`/`activeMegaForm`/`ability`/`item`/`nature`/`points`만 옮기고 나머지는 `EMPTY_MATCHUP_SLOT` 기준으로 리셋.
- 스모크 테스트(브라우저, 두 화면 다 새로 띄운 서버로 확인): 대전 로그 페이지에서 샘플 불러오기 → 포켓몬+기술까지 정확히 복원. 매치업 페이지에서 같은 샘플 불러오기 → 포켓몬은 복원되지만 "기술 선택"은 비어있는 상태(의도대로 기술 미포함) 확인.

### 1-4. ~~대전 로그 UI 문구 전면 정비~~ — **Phase 6.5로 분리(2026-08-27)**

Phase 5에서 특성/기술 로직을 워낙 많이(70종 넘게) 새로 붙이면서 `BattleLogPage.tsx`의 로그 문구가 그때그때 개별적으로 추가돼왔다 — 한 번에 모아서 톤·형식·정보량을 통일하는 리뷰가 필요하다는 사용자 판단으로 Phase 6에 편성됐었다.

사용자 재확인 결과, §1-4는 §3 제안 순서상 다음 작업이었지만 착수 전 재검토에서 Phase 6.5로 옮기는 게 낫다고 판단해 이관했다. 내용은 그대로 [phase6.5-plan.md](phase6.5-plan.md) §4 참고.

## 2. MVP 완성도 다듬기 (신규, 사용자 확정 2026-08-26)

사용자가 이번에 Phase 6의 성격을 확정했다: "전체적으로 다시 한번 데이터나 기능들을 다듬고, 기타 작은 버그들 처리, UI변경 등 MVP의 완성도를 다듬는 페이즈." 아래는 착수 시점 기준으로 이미 알려진 정비 후보다 — Phase 5 §5(발견된 버그)와 같은 패턴으로, 작업하면서 계속 채워나간다.

### 2-1. 알려진 데이터/로직 정비 후보

- ~~**우격다짐 도구 상호작용 미반영**~~ — **완료(2026-08-26)**. 생명의구슬 반동 면제만 실제로 구현 대상이었다(이젝트버튼/타면목걸이는 이 프로젝트 아이템 로스터에 아직 없는 도구라 해당 없음). Bulbapedia로 실제 상호작용을 확인한 결과 "우격다짐이 이번 기술에서 실제로 발동했을 때만(=`sheerForceAbilityName`이 채워졌을 때만) 생명의구슬 반동이 면제된다"가 본가 규칙 — `battleSimulator.ts`의 생명의구슬 반동 조건에 `!sheerForceAbilityName` 한 줄만 추가. 위력 상승은 그대로 받으면서 반동만 사라지는 것, 우격다짐 없는 대조군은 반동이 정상 발생하는 것 스모크 테스트로 확인.
- ~~**매치업 페이지(1턴 스냅샷)에 반영 안 된 축들 재검토**~~: 천진/성스러운칼(랭크 무시), 매지션/곡예(도구 강탈), 곡예(스피드 2배), 고스트다이브(방어류 차단) — **Phase 6.5로 분리(2026-08-26)**, 아래 §4 참고.
- ~~**잠꼬대 후보 제외 로직의 정확도**~~ — **완료(2026-08-26)**. 텍스트 기반 전수 조사로 "chargeTurn·usageCondition 어느 쪽으로도 안 걸러지는 본가 제외 목록" 2개(트림·흉내쟁이)를 찾아 `Move.excludedFromSleepTalk` 필드를 신설해 배선. **부수 발견 버그**: 메테오빔이 실제로는 2턴 차지 기술인데 `chargeTurn` 필드가 안 붙어 있어서(효과 텍스트만 있고 실제로는 매턴 즉시 발동하던 상태) 정정 — `chargeTurn: true` 추가. 스모크 테스트로 트림/흉내쟁이/메테오빔 셋 다 잠꼬대 후보에서 제외되는 것, 일반 기술(양날박치기)은 정상적으로 후보에 남는 것 확인.
- ~~**기습(Move.usageCondition: "opponent-damaging-move-only")의 범위 밖 케이스**~~ — **재검토 결과 수정 불필요로 확인·종결(2026-08-27)**. Bulbapedia 확인 결과 "Sucker Punch only depends on the move the target has selected, and is not affected by any consideration of whether the attack was actually executed, such as due to status conditions like sleep or freeze, or Abilities like Truant stopping the execution of a move" — 즉 본가에서도 기습은 상대가 그 턴에 **선택한** 기술의 카테고리만 보고 판정하며, 그 기술이 마비 완전마비·잠듦·냉동·트루언트 등으로 실제로 실행되는지는 전혀 고려하지 않는다. 이 프로젝트의 기존 구현(`defenderMove.category`만 확인)이 이미 본가 규칙과 정확히 일치했던 것 — "범위 밖"이라고 우려했던 케이스가 애초에 버그가 아니었다. 코드 변경 없음, 판정 순서도 바꿀 필요 없음.
- **로스터/데이터 규모 문서화 최신화 습관**: README의 데이터 규모 절이 실제 로스터보다 한참 낮게 적혀 있던 걸 이번에 발견해서 갱신했다(42종/393개/78개/96종/21종) — 앞으로 로스터·기술·특성·도구를 추가할 때마다 README도 같이 갱신하는 걸 점검 항목에 넣는다.
- ~~**멘탈허브(도구) 로직 미구현**~~ — **완료(2026-08-26)**. 실제로 이 로스터에서 걸 수 있는 4개(헤롱헤롱·도발·사슬묶기·앙코르)만 대상으로 삼았다(연속불가·회복봉인은 이 로스터에 해당 기술 자체가 없어 대상 없음). `Item.curesMentalVolatilesOnInflict` 신설, 시몬열매(혼란 즉시치료)와 같은 "걸리는 순간 치료+소모" 패턴 재사용. 4개 발동 지점(범용 inflictsVolatile 루프의 attract/taunt, 사슬묶기·앙코르 전용 분기, 헤롱헤롱바디 hitTrigger) 전부에 배선. 스모크 테스트로 4개 상태 전부 멘탈허브 보유 시 안 걸리고 아이템이 소모되는 것, 도구 없는 대조군은 정상적으로 걸리는 것 확인.
- **길동무(Destiny Bond) 신설** (사용자 1차 정리, 2026-08-27) — `moves.json`에 데이터는 있지만(`효과` 텍스트만) 실제 로직이 없음 확인. 요구사항: (1) 사용한 포켓몬을 "길동무" volatile 상태로 만듦(다른 기술을 쓸 때까지 유지), (2) 길동무 상태인 동안 자신이 그 턴에 쓰러지면 상대도 같이 쓰러뜨림, (3) 연속 사용 시 실패(방어류 `protectStreak`와 같은 패턴 — 길동무-길동무-길동무는 성공/실패/성공), (4) 승패 판정은 "먼저 HP 0이 된" 길동무 사용자가 패배. 다른 1차 정리 항목과 달리 완전 신규 메커니즘이라 규모가 크지만, 사용자 확인으로 별도 문서 없이 Phase 6에서 소화하기로 함(2026-08-27).
- ~~**파티 빌더 — 중복 방지 검증 2건**~~ — **완료(2026-08-27)**. 세부 UX는 "선택 자체를 막는다(비활성화)"로 확정 — 저장 시점에 경고하는 대신, 애초에 고를 수 없게 하는 쪽이 실수를 원천 차단한다고 판단.
  - `PokemonPickerModal`/`ItemPickerModal`에 각각 `usedPokemonIds`/`usedItemIds`(현재 편집 중인 슬롯 자신은 제외한, 파티의 다른 슬롯들이 쓰는 id 목록) prop 신설 — 목록에 있으면 항목을 `disabled` + 흐리게 표시 + "다른 슬롯에 있음" 배지로 바꾼다(포켓몬 도구 도감과 무관하게 이 두 모달 전용). `PartyBoard.tsx`가 `slots`에서 이 목록을 계산해 넘긴다.
  - 두 prop 다 optional이라(안 넘기면 아무도 안 막힘) 대전 로그/매치업 페이지의 같은 모달 재사용에는 영향 없음 — 그 화면들은 "파티" 개념이 아니라 애초에 넘기지 않음.
  - **범위 밖으로 남긴 것**: 저장된 파티/샘플 프리셋을 불러오는 경로(`loadSlots`/`loadSlot`)는 이번 검증을 거치지 않는다 — 과거(이 기능 이전)에 저장해둔 프리셋에 중복이 이미 들어있었을 수 있고, 그 경로까지 막으려면 확인 다이얼로그 흐름을 새로 설계해야 해서 범위를 피커 UI로만 한정했다.
  - 스모크 테스트(브라우저): 슬롯 3에 이미 있는 포켓몬(한카리아스)을 다른 빈 슬롯의 포켓몬 선택 모달에서 검색 → 비활성화 + "다른 슬롯에 있음" 표시 확인. 슬롯 4가 이미 지닌 도구(먹다남은음식)를 다른 슬롯의 도구 선택 모달에서 검색 → 동일하게 비활성화 확인.
- ~~**따라큐(Mimikyu, #0778) 로스터 추가**~~ — **데이터 추가 완료(2026-08-27)**. `pokemon.json`(고스트/페어리, 종족값 55/90/80/50/105/96, 특성 "탈" 단일·숨겨진 특성 없음, genderCategory "both" — Bulbapedia 성비 1:1 확인, 기술 60개)·`abilities.json`("탈" 신규)·`moves.json`("우드해머" 신규 — 로스터에 아예 없던 기술이라 같이 추가, 풀/물리/120/100/PP15/반동 1/3/접촉, Bulbapedia로 수치 확인) 갱신. README 데이터 규모 절도 최신화(43종/394개/79개). **범위 밖으로 남긴 것**: "탈"(Disguise) 특성의 실제 배틀 메커니즘("데미지를 주는 공격을 1회 무효화, 벗겨지면 최대 HP 1/8 데미지")은 이번엔 데이터만 등록하고 로직을 안 붙였다 — 길동무와 같은 패턴으로 미구현 상태 그대로 남겨둠. 스모크 테스트로 포켓몬/특성/기술 60개 전부 정상 조회되는 것, `createFighterState`로 실제 파이터 상태 생성이 에러 없이 되는 것 확인.
- ~~**번치코(Blaziken, #0257) 로스터 추가**~~ — **데이터 추가 완료(2026-08-27)**. `pokemon.json`(불꽃/격투, 종족값 80/120/70/110/70/80, 특성 "맹화"·숨겨진 특성 "가속", genderCategory "both", 기술 66개)에 메가진화 1폼(메가번치코: 80/160/80/130/80/100, 특성 "가속") 포함해 추가. `abilities.json`("가속" 신규)·`items.json`("번치코나이트" 메가스톤 신규, 다른 메가스톤과 같이 description은 비워둠 — 포켓몬 도구 도감이 로스터 역탐색으로 대신 보여줌)도 갱신. 기술표 아티팩트(사용자가 정리해둔 것)로 대조하는 과정에서 사용자가 붙여넣은 원문 중 "메가톤 버티기"·"승부굳 크러뱃"이 각각 "메가톤킥+버티기", "승부굳히기+애크러뱃"이 복사 과정에서 붙어버린 것임을 확인해 정정(둘 다 이미 로스터에 있는 기술이라 신규 데이터 추가는 없었음). README 데이터 규모 절도 최신화(44종/80개/97종, 메가진화 가능 22종). ~~**범위 밖으로 남긴 것**~~: "가속"(Speed Boost) 특성의 실제 배틀 메커니즘은 로스터 추가 당시엔 데이터만 등록했으나, 아래 §2-1 "탈·가속 특성 로직 구현"에서 마저 붙였다.
- ~~**탈(Disguise)·가속(Speed Boost) 특성 로직 구현**~~ — **완료(2026-08-27)**. 따라큐·번치코 로스터 추가 당시 데이터만 등록해두고 미뤄뒀던 두 특성의 실제 배틀 메커니즘을 마저 구현.
  - **탈**: `Ability.negatesFirstHitThenRecoils`(신규, `{recoilFraction}`) 신설. 데미지가 실제 HP로 들어가는 유일한 지점인 `applyDamageToDefender`(대타출동과 같은 choke point)에 배선 — `BattleFighterState.disguiseBroken`(unburdenActive와 같은 "배틀 끝까지 유지" 패턴)이 아직 false일 때만 데미지를 무효화하고 최대 HP 1/8 반동을 즉시 적용, 이후로는 정상 피격. Bulbapedia로 다단히트 상호작용(첫 타만 무효화, 나머지 타수는 정상 데미지 — 8세대부터 벗겨지는 즉시 반동 적용) 확인 후 반영.
  - **가속**: `Ability.boostsSpeedEachTurnEnd`(신규) 신설. 턴 종료 처리 루프(먹다남은음식·젖은접시와 같은 위치)에 배선 — 배틀 첫 턴(turnNumber===1, "등장한 턴"에 대응)은 건너뛰고 2턴째부터 매 턴 종료 시 스피드 +1랭크.
  - 대전 로그 UI: 탈 무효화+반동 전용 줄(대타출동 문구와 같은 위치·패턴), 가속 발동 시 턴 종료 로그에 "OO의 가속! 스피드가 올라갔다!" 줄 추가 — 로그 문구 전면 정비(Phase 6.5 §4)와는 별개로, 기능이 눈에 보이게 하는 최소 문구만 추가.
  - 스모크 테스트: 탈 첫 타 무효화+최대HP 1/8 반동, 2턴째부터 정상 피격, 다단히트(록블라스트) 첫 타만 무효화·나머지 타수 정상 합산, 대조군(탈 없는 포켓몬) 정상 피격 확인. 가속 1턴째 미발동, 2·3턴째 매턴 +1(누적 +2), 대조군(가속 대신 맹화를 낀 번치코) 미발동 확인.

### 2-2. UI/UX 개선 후보

- §1-4(로그 문구 정비)와 연계해서, 전반적인 UI 리뷰(레이아웃·반응형·접근성 등)가 필요하면 여기 추가.
- (실사용하면서 발견하는 것들을 계속 채워나간다)
- **파티 빌더 페이지 UI 정비 4건** (사용자 1차 정리, 2026-08-27): (1) 샘플저장 버튼이 슬롯 삭제(X) 버튼과 너무 붙어있어 5px 이동, (2) 타입 상성 요약 컴포넌트 UI 문구 수정("공격 타입별로 파티가 얼마나 약한지/버티는지..." → "현재 파티의 약점 타입을 한눈에 볼 수 있습니다..."), (3) 타입 선택 시 하단에 뜨는 컴포넌트 문구 수정("[타입] 공격에 대한 파티원 반응" → "[타입] 기술에 대한 포켓몬 상성"). 전부 트리비얼.
- **포켓몬 도감 페이지 — 방어 상성 타입 표기 추가** (사용자 1차 정리, 2026-08-27): 지금은 타입·종족값·특성·메가진화·기술만 표시하고 방어 상성(4배 약점 등)이 없음. 파티 빌더의 타입 상성 요약 컴포넌트 로직을 재사용할 수 있을지 확인 필요 — 중간 규모.
- **기술표 페이지 — 연속 공격 태그 추가** (사용자 1차 정리, 2026-08-27): `classification` 배열에 이미 "베기" 등 태그 표시 UI가 있음 — 2회 고정 연속기·2~5회 연속기(`minHits`/`maxHits`)에 태그만 추가하면 됨. 트리비얼.

### 2-3. 버그 수정

- (Phase 5 §5와 동일한 패턴 — 사용자 리포트나 작업 중 발견되는 대로 이 절에 기록한다. 착수 시점엔 알려진 게 없음)
- **타입 상성 요약 컴포넌트 자동 스크롤 버그** (사용자 1차 정리, 2026-08-27): 타입을 선택하면 하단에 나타나는 결과로 페이지가 자동 스크롤되는데, 기존에 골라둔 타입보다 더 많은 파티원이 걸리는 타입을 고르면(컴포넌트 높이가 늘어나서) 페이지 하단까지 스크롤이 안 되는 문제. 언제나 페이지 최하단까지 스크롤되도록 수정.
- ~~**일렉트로빔(Move) chargeTurn 필드 누락**~~ — **완료(2026-08-27)**. `moves.json`에 `chargeTurn: true` + `chargeSkipWeather: "비"` 추가. 고치면서 **부수 발견**: 메테오빔·일렉트로빔 둘 다 "1턴째 자신의 특공을 올리고"라는 효과 텍스트가 있는데, 실제로는 차징 턴에 어떤 스탯 변화도 적용되지 않고 있었다(Phase 6 §2-1에서 메테오빔에 `chargeTurn: true`만 붙였을 때는 이 스탯 상승 자체가 통째로 빠진 걸 못 봤던 것) — `Move.chargeStatChanges`(신규 필드, `battleSimulator.ts`의 차징 분기에서 `statChanges`와 별도로 적용 — 2턴째 공격 판정에서 다시 적용되면 안 되기 때문에 필드를 분리)를 신설해 두 기술 모두에 배선. `chargeSkipWeather`로 준비 턴이 생략되는 경우(비 오는 일렉트로빔)에도 능력치 상승은 그대로 적용됨을 확인. 스모크 테스트(`tsx`로 배틀 로직 직접 실행): 일렉트로빔 평상시 1턴째 차징+특공+1, 2턴째 발동(중복 상승 없음), 비일 때 1턴 만에 발동+특공+1+데미지 발생, 메테오빔도 1턴째 특공+1 확인, 대조군(공중날기 — 스탯 변화 없는 차지기)은 영향 없음 확인.

## 3. 제안 순서 (초안)

1. ~~헤롱헤롱 성별 데이터 정책 확인 → 구현(§1-1)~~ — **완료(2026-08-26)**, 가장 오래(Phase 4.5부터) 미뤄진 항목이었다
2. ~~샘플파티·샘플 저장 기능(§1-2·1-3)~~ — **완료(2026-08-27)**
3. ~~대전 로그 UI 문구 정비(§1-4)~~ — **Phase 6.5로 분리(2026-08-27)**
4. ~~기습의 범위 밖 케이스(§2-1)~~ — **재검토 결과 수정 불필요로 종결(2026-08-27)**, 아래 §4 참고
5. §2 나머지 정비 후보 — 우선순위는 진행하면서 사용자와 그때그때 조율

Phase 4.5·5 내내 그랬듯, 실제로 작업하다 보면 우선순위가 바뀔 가능성이 높다 — 그때그때 재조정한다. 실제로는 사용자가 §2 정비 후보를 §1보다 먼저 진행하기로 함(2026-08-26) — 자동으로 처리 가능한 것부터 먼저 끝내두면 나중에 한 번에 검토할 수 있다는 이유.

## 4. §2-1 중 아키텍처 재검토가 필요했던 2건 — 결론

2026-08-26에 §2-1 정비 후보를 훑으면서 나머지 항목(우격다짐 도구 상호작용·잠꼬대 정밀화·멘탈허브)은 자체적으로 완결했지만, 아래 2건은 "고치면 더 정확해지지만 판정 순서/구조 자체를 바꿔야 하는" 성격이라 섣불리 손대지 않고 재확인만 하고 남겨뒀었다. 둘 다 결론이 났다.

- ~~**매치업 페이지 미반영 축**~~ — **Phase 6.5로 분리(2026-08-26)**. 사용자 확인: 매치업 페이지도 "1대1 대결" 페이지인 이상 반영돼야 하는 로직은 전부 반영돼야 한다 — 단, 토글(켜고 끄기)로 "이전 턴에 이런 일이 있었다고 가정"을 사용자가 직접 선택하는 방식으로. 여기에 스피드 비교 기능 신설까지 묶어서 범위가 커져 별도 문서로 분리했다. 자세한 내용은 [phase6.5-plan.md](phase6.5-plan.md) 참고.
- ~~**기습의 범위 밖 케이스**~~ — **재검토 결과 수정 불필요로 확인·종결(2026-08-27)**. 당초 우려는 "상대가 실제로 데미지 기술을 쓸 수 있는 상태인지(상태이상으로 행동 자체가 막히는 경우 포함)까지 반영하려면 판정 순서 자체를 바꿔야 한다"였는데, Bulbapedia로 본가 규칙을 먼저 확인해보니 **애초에 반영할 필요가 없는 것으로 확인됐다** — 본가에서도 기습은 상대가 그 턴에 "선택한" 기술의 카테고리만으로 판정하고, 그 기술이 마비/잠듦/냉동/트루언트 등으로 실제 실행되는지는 전혀 고려하지 않는다(Bulbapedia: "Sucker Punch only depends on the move the target has selected, and is not affected by any consideration of whether the attack was actually executed"). 이 프로젝트의 기존 구현(`battleSimulator.ts`의 `defenderMove.category`만 확인)이 이미 정확했다. 판정 순서 변경도, 코드 수정도 불필요 — 조사만으로 종결.
