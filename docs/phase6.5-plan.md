# Phase 6.5 기획 — 매치업 페이지 개선 및 로그 문구 정비 — **완료(2026-08-28)**

> 이어서 읽기: [Phase 6 기획](phase6-plan.md)
> Phase 6 §2-1 정비 후보 중 "매치업 페이지에 반영 안 된 축들"을 사용자와 논의하면서, 이 페이지 자체를 손보는 작업으로 범위가 커져 별도 페이즈로 분리했다(2026-08-26). 이후 Phase 6 §1-4(대전 로그 UI 문구 정비)도 착수 직전 사용자 재검토로 이 문서에 합류했다(2026-08-27, §4 참고) — 서로 직접 연관은 없지만 둘 다 "기존 걸 다듬는" 성격이라 같이 묶었다.

> **종료 정리(2026-08-28)**: 활성 범위(§1·§2·§5·§6-1·§6-2 ①②③④⑥⑦⑧·§6-3)를 전부 구현·검증 완료하고 페이즈를 닫았다. 다음 페이즈로 이월: **§4 대전 로그 UI 문구 전면 정비**(§6-1 "차징 기술 전용 문구" 포함), **§8 매직미러·진영 설치 기술 스택/제거/등장 데미지**(파티 선출·교체 기능 도입이 선행돼야 함). §7 길동무는 Phase 6에서 이미 구현됨.

## 0. 배경

Phase 6에서 매치업 페이지(`MatchupPage.tsx`, `evaluateSlotMatchup`)에 반영 안 된 축 4개(매지션/곡예 도구 강탈, 곡예 스피드 2배, 고스트다이브 방어류 무시)를 발견했다. 이 프로젝트에는 페이지가 두 개 있다는 걸 먼저 정리해둔다:

- **대전 로그 페이지**(`BattleLogPage.tsx`, `runTurn`): 턴을 실제로 하나씩 진행시키는 배틀 시뮬레이터. 매 턴 상태(도구를 뺏겼는지, 직전에 무슨 기술을 썼는지 등)를 그대로 들고 가기 때문에 위 4개 효과가 이미 정상 반영돼 있다 — **손댈 것 없음**.
- **매치업 페이지**(`MatchupPage.tsx`, `evaluateSlotMatchup`): "결정력 vs 내구력" 한 방 비교 계산기. 턴 개념이 없는 스냅샷이라 "직전 턴에 뭘 했는지"에 의존하는 축이 반영될 자리가 없다.

**사용자 확인(2026-08-26)**: 매치업 페이지도 어디까지나 "1대1 포켓몬 대결"을 상정한 페이지이므로, 1대1 대결에서 실제로 반영돼야 하는 로직은 전부 반영돼야 한다는 게 사용자 의견 — 단, 이 축들은 "이전 턴에 뭘 가정하느냐"에 따라 결과가 달라지므로 **토글(켜고 끄기)로 사용자가 직접 가정을 선택**하는 방식으로 넣기로 확정했다.

## 1. 토글형 "이전 턴 가정" — **완료(2026-08-27)**

매치업 페이지 양쪽 슬롯에 각각 다음 토글을 추가했다(기본값은 전부 꺼짐 = 지금과 동일한 계산). `MatchupSlot`에 상태 필드, `MatchupSlotCard`에 체크박스, `MatchupPage`가 `evaluateSlotMatchup`/`evaluateSpeedMatchup` 옵션으로 전달.

- ~~**매지션/곡예로 상대 도구를 뺏었다고 가정**~~ — **완료**. `itemStolenFromOpponent`. 켠 슬롯이 상대 도구를 장착하고 상대는 무도구가 된 것으로 계산(양쪽 다 켜면 공격 슬롯 우선).
- ~~**곡예 발동 직후(스피드 2배)라고 가정**~~ — **완료**. `unburdenAssumed`. 켠 슬롯의 실효 스피드를 2배로 계산(§2 스피드 비교와 연동).
- ~~**고스트다이브 2턴째(방어류 무시)라고 가정**~~ — **드롭(2026-08-27)**. 매치업 페이지엔 방어/판별 개념 자체가 없어 계산할 대상이 없음. 사용자 확인 후 제외.
- ~~**성묘 배율 x0/x1/x2**~~ — **완료**(§5에서 편입). `graveVisitFaintedAllies`, 위력 50/100/150.

## 2. 스피드 비교 신설 (사용자 신규 요청, 2026-08-26) — **완료(2026-08-27)**

매치업 페이지가 지금은 결정력·내구력만 비교하고(`computeOffensePower`, `computeBulkPower`, `evaluateMatchup`) 스피드는 아예 안 보여줬다. 실제로 "누가 먼저 움직이는지"가 결정력/내구력만큼 중요한 축이라 스피드 비교를 신설했다.

- ~~양쪽 슬롯의 실효 스피드(특성/도구/날씨/랭크 반영) 계산 + 선공 표시~~ — **완료**. `evaluateSpeedMatchup`(`battleSimulator`의 runTurn 스피드 공식 미러), 보드 아래 `matchup-speed-block`에 양쪽 실효 스피드 + 선공 문구.
- ~~§1 "곡예 스피드 2배" 토글 연동~~ — **완료**. `attackerUnburden`/`defenderUnburden` 옵션으로 반영.
- ~~트릭룸 가정 토글 (스피드 역전)~~ — **완료(사용자 요청, 2026-08-27)**. 보드 하단 체크박스(보드 전역, `useMatchup.trickRoom`). 켜면 우선도 동률일 때 느린 쪽이 먼저로 판정(`turnOrder.compareTurnOrder`와 동일 방향 반전), 선공 문구에 `(트릭룸)` 표기. 동속은 그대로 랜덤.
- ~~마비 0.5배 가정~~ — **완료(사용자 요청, 2026-08-27)**. 슬롯별 토글(`paralysisAssumed`), 켠 슬롯의 스피드에 0.5배(`computeStatusSpeedMultiplier` 미러). 스피드 비교에만 반영(결정력/내구력 무관).
- **대안 검토 후 기각(2026-08-27)**: 사용자가 1차 정리 항목에서 "결정력&내구력 페이지와 같은 UI를 가진 완전히 별도의 새 페이지"로 다시 요청했으나, 재확인 결과 위 계획대로 **매치업 페이지 내 통합을 유지**하기로 확정 — 새 페이지 신설 없음.
- 선제공격손톱(quick claw)은 랜덤 발동이라 매치업 스냅샷에는 반영하지 않는다.

## 3. 범위 밖 (참고)

- ~~**기습(Move.usageCondition: "opponent-damaging-move-only")의 범위 밖 케이스**~~ — Phase 6에서 재검토 결과 수정 불필요로 종결(2026-08-27). 자세한 내용은 [phase6-plan.md](phase6-plan.md) §4 참고.

## 4. 대전 로그 UI 문구 전면 정비 (Phase 6 §1-4에서 이월 → **다음 페이즈로 미룸**, 2026-08-27)

> **상태(2026-08-27, 2차 테스트 후)**: 사용자가 대전 로그를 직접 돌려보며 정리한 수정 항목(§6-2)이 늘어나면서, "문구를 전부 나열해 톤·형식을 한 번에 통일하는" 이 전면 정비는 **뒤로 미루기로 확정**. 대신 §6-2에 모인 개별 항목 중 문구 성격인 것(탈/지구력 문구, 랭크업 결과 문구, 랭크 상한 문구, 방어 실패 문구 등)은 각자 필요한 로직 수정과 함께 Phase 6.5에서 처리한다 — 전면 통일은 그 뒤에 남은 문구를 대상으로 별도 페이즈에서.

원래 Phase 6 §1-4로 편성돼 있던 항목인데, 착수 직전 사용자 재검토로 이 문서로 옮겨왔다. 매치업 페이지 개선과 직접 연관은 없지만, 두 작업 모두 "새 기능보다 기존 걸 다듬는" 성격이라 Phase 6.5에서 같이 다룬다.

Phase 5에서 특성/기술 로직을 워낙 많이(70종 넘게) 새로 붙이면서 `BattleLogPage.tsx`의 로그 문구가 그때그때 개별적으로 추가돼왔다 — 한 번에 모아서 톤·형식·정보량을 통일하는 리뷰가 필요하다.

- **지금 상태**: 로그 문구가 최소 6가지 서로 다른 패턴으로 흩어져 있다 — `STATUS_ONSET_TEXT`/`STATUS_TRIGGER_TEXT`/`STATUS_CURE_TEXT`(상태이상 3단계, 함수 맵), `VOLATILE_LABELS`(행동방해류, 칩 전용 라벨), 메인 라인에 인라인으로 박힌 `· OO 발동!` 조각들(수십 개), 특성 전용 문구(예: 흑안개 "양쪽의 능력 변화가 전부 원래대로 돌아갔다!", 대타출동 "대타가 대신 맞았다!"), 방어류 전용 문구, `moveRestrictionMessage`(도발/사슬묶기/앙코르). 특성이 발동했을 때 "OO의 [특성명]!"으로 시작하는 것도 있고 그냥 결과만 말하는 것도 있어 형식이 일관되지 않다.
- **필요한 작업**: (1) 전체 로그 문구를 한 번에 나열해서 톤(예: 전부 "OO의 [원인]! [결과]" 형식으로 통일할지)·정보량(포켓몬 이름을 매번 반복할지, 대명사로 줄일지)·기절 시 문구(현재 `battle-fighter-fainted`로 " (기절)"만 붙는데 더 명확한 문구가 필요한지)를 검토. (2) 함수 맵 방식(STATUS_*_TEXT)과 인라인 조각 방식이 혼재하는데 특성/기술이 계속 늘어날 걸 감안하면 어느 한쪽으로 통일하는 게 유지보수에 유리한지 결정. (3) 실제 대전 로그를 몇 판 돌려보면서 정비.
- **범위**: 로직 자체는 건드리지 않고 표시 문구만 다듬는 순수 UI 작업이라 회귀 위험은 낮지만, 문구가 수십 곳에 흩어져 있어 손이 많이 간다.

## 5. 매치업 페이지 추가 정비 후보 (사용자 1차 정리, 2026-08-27)

`docs/Phase6-테스트 중 발견한 수정할 점.txt`로 사용자가 실사용하며 정리해온 결정력&내구력 페이지 관련 항목들. 이 페이지 자체가 이미 Phase 6.5 대상이라 여기로 편입한다. 우선순위는 미정 — 착수 시 조율.

- ~~**UI 문구 수정**~~ — **완료(2026-08-27)**. `MatchupPage.tsx` 헤더 문구를 "…기술을 선택하면 타수 판정을 확인할 수 있습니다."로 교체.
- ~~**실능치 카드에 랭크 변화 미반영**~~ — **완료(2026-08-27)**. `MatchupSlotCard`의 실능치 칩이 `rankStageMultiplier(slot.stages[stat])`를 반영하도록 수정(랭크 변화 시 칩 강조 + `기본 N (±n랭크)` 툴팁). **도구(구애머리띠 등)·특성(가속 등)은 계산식 배율이거나 턴마다 변하는 값이라 칩에는 반영하지 않기로 함** — 이 축들은 이미 결정력/내구력/스피드 출력에 반영돼 있어 칩에까지 넣으면 이중 표시가 됨.
- ~~**물리/특수 내구력 UI 추가**~~ — **완료**(이전 페이즈에서 처리). 방어측 카드에 물리내구/특수내구 수치 표시.
- ~~**날씨 배경 표시**~~ — **완료(2026-08-27)**. `battleBoardBackground`의 틴트 로직을 `lib/environmentBackground.ts`(`environmentTintBackground`)로 빼내 두 페이지가 공유. 매치업 보드도 날씨/필드 색으로 물든다. **덤으로 날씨 데미지 배율(비=물 1.5배·불 0.5배, 쾌청=불 1.5배·물 0.5배)이 매치업 결정력에 아예 미배선이던 것도 이때 같이 연결**(엔진 `getWeatherDamageMultiplier` 미러) — 필드 배율만 넣고 날씨 배율은 빠지면 오히려 버그처럼 보여서.
- ~~**필드 선택 UI 신설**~~ — **완료(2026-08-27)**. `FieldPicker` 신설(WeatherPicker와 동형), `useMatchup`에 `field` 상태 추가, 매치업 보드 배경에 필드 색 틴트. `evaluateSlotMatchup`이 필드 배율을 반영: 그래스/사이코/일렉트릭필드 해당 타입 1.3배, 미스트필드 드래곤 0.5배, 대지의파동(필드 타입/위력 변경)·미스트버스트·와이드포스·라이징볼트(필드 위력 배가)까지 엔진과 동일하게 처리. 필드는 스피드에 영향 없어 `evaluateSpeedMatchup`은 불변.
- ~~**성묘 배율 x0/x1/x2 토글**~~ — **완료(2026-08-27)**. §1(이전 턴 가정 토글)에 편입해 구현(`graveVisitFaintedAllies`, 위력 50/100/150).
- ~~**부자유친이 결정력에 미반영**~~ — **완료(2026-08-27)**. `evaluateSlotMatchup`에서 공격측 특성이 `followUpHitPowerMultiplier`를 가지면(단일타·비고정데미지 기술 한정) 위력 `+ round(위력 × mult)`로 추가타를 합산. 엔진 조건과 동일.
- ~~**다단히트(2~5회) 결정력 세부 표시**~~ — **완료(2026-08-27)**. `minHits`/`maxHits` 기술(록블라스트류, `multiHitPowers` 없음)도 x2~x5 타수 pill을 띄우고, `evaluateSlotMatchup`이 `위력 × 선택 타수`(min~max로 클램프)로 결정력을 계산. 고정 타수(더블어택 등 min===max)는 pill 없이 항상 반영.
- ~~**스크린(리플렉터·빛의장막·오로라베일) 가정**~~ — **완료(2026-08-27, 사용자 요청)**. 방어(상대) 슬롯 카드에 `없음/리플렉터/빛의장막/오로라베일` pill 신설(`MatchupSlot.screen`, `useMatchup.setScreen`). `evaluateSlotMatchup`이 `SlotMatchupOptions.screen`을 받아 해당 카테고리 데미지를 절반으로(내구력 ×2) — `battleSimulator` resolveAction의 `screenMultiplier` 미러(reflect=물리, lightScreen=특수, auroraVeil=둘 다). 두꺼운지방·열매 등 기존 내구 배율과 **곱**으로 합쳐지고(덮어쓰기 아님), 공격측이 틈새포착(`bypassesScreensAndSubstitute`)이면 무시. 스피드 비교엔 영향 없음. 방어측 카드의 기본 물리/특수내구 칩은 "raw 기준값"이라 스크린을 반영하지 않는다(결정력·타수 판정에만 반영).

## 6. 대전 로그 페이지 추가 정비 후보

### 6-1. 1차 정리 (2026-08-27)

1차 정리 파일에서 대전 로그 페이지 관련 항목. §4(문구 전면 정비)가 뒤로 밀리면서, 여기 문구 항목들도 §4를 기다리지 않고 §6-2와 함께 Phase 6.5에서 개별 처리한다.

- ~~**랭크업 로그 표기**~~ — **완료(2026-08-27, 커밋 `347122c`)**. 랭크업 기술 사용 시 결과 문구가 아예 없던 것 → §6-3 형식(`selfStatRises`)으로 렌더. **§6-2 ⑦에서 통합 구현**.
- ~~**실능치 UI에 랭크 변화 반영(UX)**~~ — **완료(2026-08-27, 커밋 `041840f`)**. **§6-2 ⑧과 동일 항목**.
- ~~**특성 발동 문구(깨어진 갑옷 등) 부재**~~ — **완료(2026-08-27)**. 깨어진갑옷(Weak Armor) 로직은 이미 배선돼 있었음(`abilities.json` hitTrigger: `def -1` / `spe +2`). ③ 지구력에서 만든 `triggerAbilityHitEffect`의 랭크 변화 감지 경로가 "오른 것만" 기록하던 걸, **오름·내림을 각각 `abilityRaisedDefenderStats`/`abilityLoweredDefenderStats`로 나눠 담도록** 확장. `BattleLogPage`가 두 줄로 렌더 — `[ OO의 깨어진갑옷! OO의 방어가 내려갔다! ]` + `[ OO의 스피드가 크게 올라갔다! ]` (내림 줄에서만 특성 이름을 알리고, 오름 줄은 결과만). 지구력처럼 오르기만 하는 케이스는 종전과 동일하게 한 줄.
- **차징 기술 전용 문구(공중날기 등)**: 순수 문구 — **§4(전면 정비)로 이월**(Phase 6.5 범위 밖).
- ~~**판별/방어류 문구 버그**~~ — **완료(2026-08-27, 커밋 `347122c`)**. 1차엔 "차징 기술과 판별이 겹칠 때 문구가 이상함"이라고만 적혀 있었는데, 2차 테스트에서 재현 시나리오가 구체화됨(방어 + 랭크업 기술) → **§6-2 ⑦에서 처리**. "방어가 막을 수 없는 것(자기 대상 랭크업)을 막은 것처럼 문구가 뜨던" 버그를 `isOpponentTargetingMove` 게이트 + `protectFailed` 경로로 수정.
- ~~**일렉트로빔 chargeTurn 누락**~~ — **완료(2026-08-27)**, Phase 6 §2-1에서 처리(메테오빔도 같이 발견된 스탯 상승 누락 버그를 함께 수정). 자세한 내용은 [phase6-plan.md](phase6-plan.md) §2-3 참고.

### 6-2. 2차 테스트에서 추가 (사용자 실사용, 2026-08-27)

사용자가 대전 로그를 직접 돌려보며 정리한 항목. 각 항목 뒤 `[조사 결과]`는 착수 전 코드 확인 메모. **① ② ③ ⑥ ⑧은 소규모(로직 한 줄 또는 문구/표시만), ④ ⑦은 중간 규모, ⑤는 향후(§8)로 분리.**

> **상태(2026-08-28)**: ①②③④⑥⑦⑧ 전부 완료. ⑤만 §8로 이월(교체 도입 후). 커밋 — ①②⑧ `041840f`(묶음 A), ⑥⑦ `347122c`(묶음 B), ③ `8ed34a3`, ④ `324a127`.

- ~~**① 특성 가속 — 첫 턴부터 적용**~~ — **완료(2026-08-27, 커밋 `041840f`)**. 대전 로그는 교체 없는 1대1 대면 전용이므로, 첫 턴도 "교체로 나온 턴"이 아니다 → 가속은 첫 턴 종료부터 발동해야 함. (파티 선출 시뮬레이터로 확장하면 그때 교체턴 예외를 되살린다.)
  - `[조사 결과]` `battleSimulator.ts` ~2786 `if (fighterAbility?.boostsSpeedEachTurnEnd && state.turnNumber > 1 && ...)` — `state.turnNumber > 1` 가드가 원인. **처리**: 그 조건만 제거(현재 `if (fighterAbility?.boostsSpeedEachTurnEnd && !isFainted(fighter))`), 주석도 1v1·교체 없음 근거로 갱신.
- ~~**② 특성 탈(따라큐) — 문구 교체**~~ — **완료(2026-08-27, 커밋 `041840f`)**. 현재 `[ 따라큐의 탈! 공격이 무효화되었다! 따라큐는 반동으로 16 데미지를 입었다! ]` → "공격이 무효화되었다!" 를 빼고 **"따라큐의 정체가 드러났다!"** 로. 최종: `[ 따라큐의 탈! 따라큐의 정체가 드러났다! 따라큐는 반동으로 16 데미지를 입었다! ]`
  - `[조사 결과]` `BattleLogPage.tsx` `hitNegatedByAbilityName` 조건 블록. 가운데 문장만 `{defenderName}의 정체가 드러났다!`로 교체.
- ~~**③ 특성 지구력 — 발동 문구 부재**~~ — **완료(2026-08-27, 커밋 `8ed34a3`)**. 데미지를 받으면 방어 +1랭크가 **실제로 적용은 되는데**(로직 정상) 로그에 아무 문구가 없어 반영 여부를 알 수 없음. → `[ 브리두라스의 지구력! 브리두라스의 방어가 올라갔다! ]` 출력.
  - `[조사 결과]` `abilities.json` 지구력 = `hitTrigger.selfStatChanges [{def:+1}]`. `triggerAbilityHitEffect`에서 `defender.stages`에 적용은 되지만(정상), 다른 hitTrigger 분기(정전기 상태이상 등)와 달리 **로그 이벤트 필드를 안 채웠다**. **처리**: `abilityRaisedDefenderStats` / `abilityLoweredDefenderStats` + `abilityRaisedDefenderStatsAbilityName` 이벤트 필드 추가, `BattleLogPage` 렌더 라인 추가(§6-1 깨어진갑옷과 공용 경로).
- ~~**④ 기술 스텔스록 — 진영 설치 + 환경 UI 영구 표시**~~ — **완료(2026-08-27)**.
  - `BattleState.stealthRock: { a, b }` 신설(매 턴 이월, 배틀 끝까지 영구). `Move.setsHazard: "stealthRock"` + `moves.json` 스텔스록에 부여.
  - `resolveAction`: 스텔스록은 **항상 상대 진영**에 깔린다(사용자 확인 — 문구 Q1 답 A). 이미 그 진영에 깔려 있으면 실패.
  - 대전 로그: 환경 태그에 `[상대 이름] 진영: 뾰족한 바위(스텔스록)` 표시. 기술 사용 줄에 `· 상대 편 필드에 뾰족한 바위가 깔렸다!` / 재사용 시 `· 이미 뾰족한 바위가 깔려 있어 실패!`.
  - **매직미러 반사는 제외** — 특성 자체가 코드에 없고, 본가 매직미러(도발·씨뿌리기 등 전 변화기 반사) 범위 조사가 필요해 후속 항목으로 분리(§8). 교체 시 등장 데미지도 §8.
- **⑤ 상대 진영 설치 기술 전반** — §8로 분리(레퍼런스 + 향후). 스택·제거(고속스핀/안개제거)·독타입 교체 해제는 전부 교체 도입 후.
- ~~**⑥ 랭크업 상한(+6) UI 문구**~~ — **완료(2026-08-27, 커밋 `347122c`)**. 현재 무한 칼춤이 가능한 것처럼 보임. → +6 도달 후 7번째 랭크업 기술을 쓰면 `[ [포켓몬]의 [능력치]는 더 이상 올라가지 않는다! ]` 출력.
  - `[조사 결과]` `statStages.ts` `clampStage`가 -6~+6으로 **값 자체는 이미 클램프됨**(무한 상승 아님). 빠진 건 "변화가 없었음"을 감지해 전용 문구를 내는 것. **처리**: `applyMoveStatChanges` 전후 stages 비교(`attackerStagesBeforeMoveChange`)로 `selfStatRises` / `selfStatsAtMax` 이벤트 필드 채움, `BattleLogPage`가 "올라갔다" 줄과 "더 이상 올라가지 않는다" 줄을 분리 렌더.
- ~~**⑦ 방어(판별) vs 랭크업 기술 매치업**~~ — **완료(2026-08-27, 커밋 `347122c`)**. 두 포켓몬이 각각 방어 / 랭크업 기술(나쁜음모·칼춤)을 쓴 턴.
  - 현재(버그): 방어 쪽 `[ 방어로 몸을 지켰다! ]`, 랭크업 쪽 `[ 방어로 막혔다! ]` — **방어는 자기 대상 랭크업을 막지 못하는데 막은 것처럼 나옴.**
  - 기대: 방어 쪽 → `[ [포켓몬]의 방어는 실패했다! ]` (막을 게 없었음). 랭크업 쪽 → 정상 결과 문구 `[ [포켓몬]의 [능력치 2종 이상이면 쉼표로 나열]가(이) [1랭크=수식어 없음 / 2랭크="크게" / 3랭크="아주 크게"] 올라갔다! ]`
  - `[조사 결과]` `battleSimulator.ts` 이미 `effect.target !== "self" && opponentEffectsBlocked` 조건이라 **자기 대상 랭크업은 방어를 무시하고 적용됨**(로직 정상). **처리**: (a) 방어측 — 상대가 낸 게 방어로 막을 수 있는 것(`isOpponentTargetingMove`)이 아니면 `protectFailed`로 처리해 "방어는 실패했다!" 출력, (b) `blockedByProtectMoveName`을 `isOpponentTargetingMove`로 게이트해 랭크업측 "막혔다" 제거, (c) §6-3 형식대로 `selfStatRises` 결과 문구 렌더 — "랭크업 로그 표기"(§6-1)를 이걸로 구현.
- ~~**⑧ 턴 진행 중 실능치 즉시 반영**~~ — **완료(2026-08-27, 커밋 `041840f`)**. 랭크업 기술을 쓰면 그 턴 진행 동안 체력바 옆 실능치 표시에 즉시 반영.
  - `[조사 결과]` `BattleLogPage.tsx` `{Math.round(fighter.realStats[key])}` — 랭크 배율 미적용. **처리**: 매치업 §5 실능치 칩과 동일 패턴(`rankStageMultiplier(stage)` 곱, HP 제외) + `is-boosted`/`is-lowered` 강조·툴팁·`battle-real-stat-stage` 배지.

### 6-3. 랭크업 결과 문구 형식 (사용자 확정, 2026-08-27) — ⑥⑦ 및 §6-1 "랭크업 로그 표기" 공통

`[랭크업 기술을 쓴 포켓몬]의 [올라간 능력치 이름, 2종 이상이면 쉼표로 나열]가(이) [폭에 따른 수식어] 올라갔다!`
- 폭 수식어: 1랭크 → (없음), 2랭크 → "크게", 3랭크 → "아주 크게".
- 조사 "가/이"는 능력치 이름 마지막 글자 받침으로 자동 판별(기존 `eunNeun` 옆에 짝이 되는 헬퍼).
- 상한 도달로 한 칸도 못 올랐을 때 → `[포켓몬]의 [능력치]는 더 이상 올라가지 않는다!` (⑥).

## 7. 길동무(Destiny Bond) 신설 — Phase 6로 배정 (사용자 확인, 2026-08-27)

1차 정리 항목 중 유일하게 완전 신규 메커니즘(전용 volatile 상태 + 승패 판정 로직 변경)이 필요해 다른 "다듬기" 성격 항목들과 규모가 다르지만, 별도 문서를 새로 만들 정도는 아니라는 사용자 판단으로 **Phase 6**에 포함하기로 확정했다. 자세한 요구사항은 [phase6-plan.md](phase6-plan.md) §2-1 참고.

## 8. 상대 진영 설치 기술 · 매직미러 (레퍼런스 + 향후) — 2026-08-27

§6-2 ⑤에서 분리. **전체 메커니즘은 파티 선출·교체가 도입된 뒤에 구현**한다(교체가 없는 현행 대전 로그에선 "등장 시 데미지/효과"가 발동할 일이 없음). 스텔스록만 §6-2 ④에서 "설치 상태 + 환경 UI 표시 + 설치/실패 문구"까지 부분 선반영했다(항상 상대 진영, 매직미러 없음).

### 매직미러(Magic Bounce) — 조사 필요 (§6-2 ④에서 분리, 2026-08-27)

현재 코드에 이 특성이 아예 없다. 본가 매직미러는 스텔스록뿐 아니라 **상대를 겨냥한 변화기 전반**(도발·씨뿌리기·도깨비불·전기자석파·짓궂은태도류·설치기 등)을 시전자에게 되돌려보낸다. 착수 전 확인할 것:
- 반사 범위를 어디까지 볼지(스텔스록만? `isOpponentTargetingMove` 전체?).
- 반사된 기술의 처리 주체(시전자가 자기 자신에게 쓴 것으로 재평가할지, 결과만 뒤집을지).
- 이 프로젝트에서 매직미러를 가질 포켓몬이 있는지(없으면 데이터는 비워두고 로직만).
- `isOpponentTargetingMove`(현재 fieldEffects에서 export, 사이코필드·방어 판정과 공유 중)를 그대로 반사 판정에 재사용할 수 있는지.

**정의**: 변화 기술 중 상대방 진영에 부정적인 장치를 설치하는 기술("장판기"). 압정류는 중복 사용으로 효과가 누적된다.

**해당 기술 6종**: 끈적끈적네트 · 독압정 · 비검천중파 · 스텔스록 · 암석액스 · 압정뿌리기

**스택 규칙**:
- **독압정**: 1장 = 일반 독, 2장 = 맹독.
- **압정뿌리기**: 1장 = 최대 HP 1/16, 2장 = 1/8, 3장 = 1/4.
- 스텔스록·끈적끈적네트 등 나머지는 1장으로 고정(중복 무의미).

**제거 방법**:
- **고속스핀 / 안개제거**: 자기 진영(고속스핀) 또는 양 진영(안개제거)의 설치물을 걷어낸다.
- **독압정 한정**: 독 타입 포켓몬이 교체로 나오면 그 즉시 사라진다.

**설치 시 문구(스텔스록, §6-2 ④에서 선반영)**: `우리 편 필드에 뾰족한 바위가 떠다니기 시작했다!`

## 9. 로스터 마무리 — 비비용(Vivillon, #0666) 추가 (2026-08-28)

Phase 6.5의 마무리로 포켓몬 1종 추가(사용자 요청). 지역별 무늬 20종은 능력치·타입·특성이 전부 동일한 코스메틱 차이라 1엔트리로만 등록.

- **`pokemon.json`**: `id`/`name` 모두 `비비용`. 벌레/비행, 종족값 80/52/50/90/50/89(합 411), 특성 `인분`·`복안` + 숨겨진 특성 `프렌드가드`, `genderCategory: "both"`(본가 성비 1:1), learnset 40종 — 전부 기존 `moves.json`에서 resolve(신규 기술 없음). 메가진화·리전폼 없음.
- **`abilities.json`**: `인분`·`복안`·`프렌드가드` 3종 신규 등록 — **설명 텍스트만, 전투 로직은 미배선**(따라큐 "탈"·조로아크 "일루전"과 동일한 데이터 우선 패턴).
  - `복안`(명중률 1.3배): 엔진에 명중률 배율 배선은 이미 있으나(`accuracyExtraMultiplier`) 특성용 필드가 없어 후속 배선 대상. 비비용의 가루기(수면가루·저리가루·독가루)가 지금은 기본 명중률로 계산된다.
  - `인분`(상대 기술 추가 효과 무효): `opponentEffectsBlocked` 경로에 방어측 특성 게이트를 새로 걸어야 함 — 후속.
  - `프렌드가드`(더블배틀 아군 피해 감소): 1대1 엔진에서는 대상 없음.
- **`README.md`**: 데이터 규모 절 최신화(포켓몬 46종 / 기술 396개 / 특성 84개).
- **검증**: learnset 40종·특성 3종 전부 resolve, `비비용` 중복 없음, `npx tsc -b` / `npx oxlint` / `npm run build` 통과.
- **사용자 원문의 stray 토큰**: 기술 목록에 `방어, 석, 벌레의저항`으로 `석`이 끼어 있었으나 대응되는 기술이 없어(복붙 아티팩트로 판단) 제외.
