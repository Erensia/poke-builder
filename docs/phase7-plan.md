# Phase 7 기획 — 배틀타워 2차 테스트 수정 & 곁다리 특성 로직

> 이어서 읽기: [Phase 6.5 기획](phase6.5-plan.md)
> Phase 6.5를 닫으면서 그 문서의 §4(대전 로그 UI 문구 전면 정비)와 §8(상대 진영 설치 기술 · 매직미러)을 이월받았다(2026-08-28). 둘 다 "6.5 범위에서 다루기엔 크거나, 선행 인프라가 필요해서" 미뤄둔 항목이다.

## 0. 이 문서의 성격

Phase 6.5는 "기존 걸 다듬는" 페이즈였고, 그 안에서 대전 로그의 개별 문구 버그·랭크업 결과 문구 같은 건 각 항목의 로직 수정과 함께 처리했다(6.5 §6-2). Phase 7은 그 나머지를 맡는다:

- **§1 배틀타워(구 대전 로그) 2차 테스트 수정사항**: 당초 "로그 문구 전면 정비"였는데, 사용자 2차 플레이테스트 노트(`docs/phase7 수정 보완할 점.txt`)를 항목화하니 **문구 표준화 + 로직 버그 수정 + 미구현 기술 3종(미러코트·기사회생·죽기살기)·모래바람 틱·미러아머 확장 + 빌더 검색 UI**까지 포함됐다. §1-3에 A~G로 정리. 선행 조건 없음 — 지금 착수.
- **§2 상대 진영 설치 기술 · 매직미러**: 스텔스록만 6.5 §6-2 ④에서 "설치 상태 + 환경 UI + 설치/실패 문구"까지 부분 선반영됐다. 설치기 6종의 스택·제거·독타입 교체 해제·등장 시 데미지는 **파티 선출·교체 인프라가 선행돼야** 한다 — 교체가 없는 현행 1대1 엔진에선 "등장 시" 이벤트가 발생할 일이 없다. **매직미러 반사는 예외로 2026-08-28에 이미 배선했다**(메가픽시 로스터 추가와 함께) — 반사는 교체 없는 대면에서도 완전히 정의되므로. §2-5 참조.
- **§3 곁다리 특성 로직**: 로스터를 넓히다 딸려온 특성 배선. 복안·페어리오라·매직가드·매직미러·인분 **완료**. 플라워베일·공생은 1대1 엔진 구조상 발동 불가라 **[Phase 7.5](phase7.5-plan.md)로 이월**.

착수 순서: **§1 먼저**(선행 조건 없음, 2차 테스트 수정사항 착수 중). §2 설치기와 §3 잔여(플라워베일·공생)는 Phase 7.5(파티 선출·교체 인프라)에서.

---

## 1. 대전 로그 UI 문구 전면 정비 (6.5 §4 이월)

원래 Phase 6 §1-4 → Phase 6.5 §4로 옮겨졌다가, 6.5에서 2차 테스트 개별 항목(6.5 §6-2)이 늘어나면서 "문구를 전부 나열해 톤·형식을 한 번에 통일하는" 이 전면 정비만 다시 미뤄졌다. 개별 문구 버그(탈/지구력 문구, 랭크업 결과 문구, 랭크 상한 문구, 방어 실패 문구, 깨어진갑옷 문구)는 6.5에서 이미 처리됐으므로, Phase 7 §1은 **그러고도 남은 문구 전체**를 대상으로 한다.

### 1-1. 배경

Phase 5에서 특성/기술 로직을 70종 넘게 새로 붙이면서 `BattleLogPage.tsx`의 로그 문구가 그때그때 개별적으로 추가돼왔다. 한 번에 모아서 톤·형식·정보량을 통일하는 리뷰가 필요하다.

### 1-2. 지금 상태 — 흩어진 문구 패턴 (2026-08-28 기준 코드 확인)

`src/components/BattleLogPage.tsx` 한 파일 안에 최소 이만큼의 서로 다른 방식이 섞여 있다:

| 방식 | 위치(대략) | 예시 |
|---|---|---|
| 상태이상 3단계 함수 맵 | `STATUS_ONSET_TEXT` / `STATUS_TRIGGER_TEXT` / `STATUS_CURE_TEXT` (`:135`~) | `"OO는 화상을 입었다!"` / `"OO는 화상 데미지를 입었다!"` |
| 행동방해류 칩 전용 라벨 | `VOLATILE_LABELS` (`:54`), `SCREEN_LABELS` (`:72`), `STATUS_LABELS` (`:45`) | 칩 UI에만 쓰는 짧은 명사(`"풀죽음"`, `"리플렉터"`) |
| 메인 라인 인라인 조각 | 로그 렌더 JSX 곳곳의 `<> · OO 발동!</>` (수십 개) | `· 매지션 발동! 상대의 OO을 빼앗았다!`, `· 상대 편 필드에 뾰족한 바위가 깔렸다!` |
| 특성 전용 문구 | 렌더 JSX 분기 | 흑안개 `"양쪽의 능력 변화가 전부 원래대로 돌아갔다!"`, 대타출동 `"대타가 대신 맞았다!"` |
| 방어류 전용 문구 | 렌더 JSX 분기 (`:754`, `:997`, `:1005`) | `"OO의 방어는 실패했다!"`, `"OO는 방어로 몸을 지켰다!"` |
| 기술 선택 제약 | `moveRestrictionMessage` (`:219`) | `"OO는 도발에 걸려 변화기를 사용할 수 없어요."` (여기만 "~해요" 말투) |
| 랭크 변화 결과 | `stageRiseAdverb` + 조사 헬퍼(`eunNeun`/`iGa`/`eulReul`/`roEuro`) 조합, 6.5에서 추가 | `"OO의 공격이 크게 올라갔다!"`, `"OO의 방어는 더 이상 올라가지 않는다!"` |

일관성이 깨지는 지점:

- 특성 발동을 **"OO의 [특성명]!"으로 시작**하는 것(지구력·깨어진갑옷·탈)과 그냥 **결과만** 말하는 것(정전기 상태이상 부여 등)이 섞여 있다.
- **포켓몬 이름을 매번 반복**한다 — 한 로그 블록 안에서 "따라큐의 탈! 따라큐의 정체가 드러났다! 따라큐는 반동으로…"처럼 3번 나오기도 한다.
- **말투**가 대부분 "~했다!"인데 `moveRestrictionMessage`만 "~해요" 체다.
- 기절 표시가 `battle-fighter-fainted` 클래스로 `" (기절)"`만 붙는다 — 별도 로그 라인이 없다.

### 1-3. 작업 목록 — 2차 테스트 수정사항 (2026-08-28 확정)

`docs/phase7 수정 보완할 점.txt`(사용자 2차 플레이테스트 노트)를 정식 항목화한 것. 문구만인 줄 알았던 §1이 실제로는 **로직 버그 수정 + 미구현 기술 3종 + 신규 UI**까지 포함한다. ❓는 없음 — 아래 결정값은 전부 사용자 확인 완료.

#### A. 파티 빌더 페이지 (UI 신규)

- **A-1** "저장된 샘플에서 불러오기" 모달에 검색창 추가.
- **A-2** "저장된 파티" 목록 UI에 검색창 추가.

#### B. 배틀타워 리네이밍

- **B-1** 페이지명 `대전 로그` → **`배틀타워`** (사이드바·헤더·라우팅 라벨 등 전부).
- **B-2** 페이지 설명문 → **`실전 배틀 시뮬레이션`** (기존 긴 안내 문장 대체).

#### C. 로그 문구 표준화 (표시층)

문구는 전부 **"~했다!/~듯하다..." 평서체**로 통일. `moveRestrictionMessage`의 "~해요" 체도 교체.

- **C-1** 데미지 0배(타입 면역·특성): `상대 [포켓몬]에게는 효과가 없는 듯하다...` 추가. (노트 앞뒤로 문구가 살짝 달랐는데 **"효과가 없는 듯하다..."로 통일** 확정.)
- **C-2** 효과 굉장(×2~×4): 데미지 줄 다음 줄에 `효과가 굉장했다!`.
- **C-3** 효과 별로(×0.25~×0.5): `효과가 별로인 듯하다...`.
- **C-4** 급소: 데미지 줄 인라인(`— 급소에 맞았다! N 데미지`)에서 **분리** → 다음 줄 `급소에 맞았다!`.
- **C-5** 명중 빗나감: `OO의 기합구슬 — 빗나갔다!` → `OO의 기합구슬 — !` + 다음 줄 `[상대]는 맞지 않았다!`.
- **C-6** 랭크**다운** 기술 결과 문구 신설(거짓울음 등). 현재 랭크업만 있음 → `OO의 특수방어가 크게 떨어졌다!`처럼 대칭 문구. `stageRiseAdverb` 재사용, 상대 대상 statChanges(delta<0)를 `battleSimulator`가 로그 필드로 넘겨줘야 함(랭크업 `selfStatRises`와 같은 패턴의 `opponentStatDrops`).
- **C-7** "이미 있어 실패"류(스텔스록 중복 등): `· 이미 뾰족한 바위가 깔려 있어 실패!` → `· 그러나 실패했다!`.
- **C-8** 이미 걸린 상태이상에 같은 상태이상 재시전: 실패 표시 없이 넘어가던 걸 → `[사용자]의 맹독 - 그러나 실패했다!`. (`inflictStatus`가 상태 변화 없음을 반환하는 경우 로그 필드로.)
- **C-9** 대타출동 실패: `· 이미 대타가 있거나 HP가 부족해 실패!` → `· 그러나 실패하고 말았다!`.

#### D. 로그 문구 — 개별 케이스

- **D-1** 차징 1턴째 문구 개별화 (공통 `준비 중! 다음 턴 발동된다` 대체):
  - 구멍파기 → `[포켓몬]은(는) 땅을 파기 시작했다!`
  - 메테오빔 → `[포켓몬]은(는) 우주의 힘을 모으기 시작했다!`
  - 일렉트로빔 → `[포켓몬]은(는) 전기를 모으기 시작했다!`
  - 공중날기 → `[포켓몬]은(는) 하늘 높이 날아올랐다!`
  - 뛰어오르기 → `[포켓몬]은(는) 하늘 높이 뛰어올랐다!`
  - 다이빙 → `[포켓몬]은(는) 물속 깊이 가라앉았다!`
  - `Move`에 `chargeMessage?: string` 추가하거나 id 매핑 테이블. (id 매핑이 로직 무변경이라 안전.)
- **D-2** 길동무 사망: `번치코는 팬텀의 길동무로 쓰러졌다` → `팬텀은 번치코를 길동무로 삼았다!` + `번치코는 쓰러졌다` (2줄).
- **D-3** 가속 6랭크 상한: 스피드 +6에서도 `가속! 스피드가 올라갔다!`가 뜨는 걸 → `OO의 가속! OO의 스피드는 더 이상 올라가지 않는다!`. (턴 종료 `speedBoostAbilityName` 이벤트에 "상한 도달" 플래그 추가.)

#### E. 로직 버그 / 미구현

- **E-1 무적 상태 + 자기 대상 기술 버그**: 상대가 공중날기/구멍파기 등으로 반쯤 숨은 상태일 때 **칼춤·방어 같은 자기 대상 기술**을 쓰면 `무적 상태라 빗나감`으로 처리돼 랭크업/방어 발동이 통째로 무산됨. → `evadedByCharge` 판정을 **상대를 겨냥하는 기술**(`isOpponentTargetingMove`)에만 적용. 자기 대상 기술은 무적과 무관하게 정상 발동.
  - 관통 데미지 배율은 대부분 이미 있음(`bypassesHiding`): 지진↔구멍파기 ×2, 파도타기↔다이빙 ×2, 번개·폭풍↔공중날기(배율 없이 명중). 확정 사항: 공중날기 상대 번개 ×2, 다이빙 상대 파도타기 ×2, 구멍파기 상대 지진 ×2. → 현재 번개/폭풍은 `hidingBypassMultiplier` 없음(×1) → **번개에 ×2 부여** 확인.
  - **떨어뜨리기**(Smack Down, 바위): 공중날기/뛰어오르기(sky)로 떠 있는 상대에게 데미지가 들어가고 **동시에 차징이 캔슬**된다. `groundsAirborne` + `cancelsTargetCharge` 신규 플래그.
- **E-2 무릎차기 반동**: 빗나감 / 상대 방어류에 막힘 / 격투 무효(고스트) 시 자기 **최대 HP 1/2**(내림) 반동. 현재 미구현. 전용 문구 `[사용자]은(는) 의욕이 넘쳐 땅에 부딪혔다!`. `Move.crashFraction?: number` 신설(0.5).
- **E-3 철제광선 반동**: 사용 시 **무조건** 자기 최대 HP 1/2 데미지(빗나가도). 현재 미구현. `Move`에 "사용 시 고정 자해" 플래그(`selfDamageFractionOnUse?: number` 또는 재사용 가능한 이름).
- **E-4 미러아머 문구 + 반사 범위**:
  - 문구 신설: `OO의 미러아머! / OO의 미러아머로 능력을 떨어뜨리는 효과를 되받아쳤다! / [상대]의 [스탯]이 크게 떨어졌다!`.
  - 반사 범위: 변화기 랭크다운(거짓울음 등)뿐 아니라 **공격 기술의 부가효과 랭크다운(브레이크클로 등)도 되받아친다** (확정). 현재 `reflectsOpponentStatDrops`가 어디까지 잡는지 코드 확인 후 보강.
- **E-5 죽기살기 로직 정정**: 현재 데이터가 `power: null` + 효과 텍스트 "HP 낮을수록 위력"(= 기사회생 설명 오배정)이고 코드 참조도 전혀 없어 사실상 미구현. **죽기살기 = Endeavor**: 상대 HP를 사용자의 현재 HP와 같게 만든다(상대 HP > 사용자 HP일 때만; 필중은 아님 명중 100). `Move.setsTargetHpToUserHp?: boolean`.

#### F. 신규 기술/특성 구현

- **F-1 미러코트**(데이터만, 로직 없음): 그 턴 받은 **특수** 데미지 ×2로 반격. 타입 상성·자속 계산 안 함. **악타입에 무효**. 시전 전 기절 시 미발동. 대타로 받은 데미지는 반사 불가. 우선도 -5는 데이터에 이미 있음. → `Move.counters` + `BattleFighterState.damageTakenThisTurn`로 구현(물리 카운터도 같은 축, 데이터만 없음).
- **F-2 기사회생**(현재 `power: null`) = Reversal 위력표(현재 HP 비율):
  | HP % | 위력 |
  |---|---|
  | 100~71 | 20 |
  | 70~36 | 40 |
  | 35~21 | 80 |
  | 20~11 | 100 |
  | 10~5 | 150 |
  | 4~1 | 200 |
  `Move.reversalPowerTable` 또는 계산 함수. (죽기살기와는 로직이 완전히 다르므로 공유 안 함.)
- **F-3 모래바람 틱 데미지**: 바위/땅/강철 타입이 아닌 포켓몬은 매턴 종료 시 최대 HP **1/16**. 매직가드·모래 관련 특성(모래숨기/모래의힘/모래날림/모래헤치기) 보유 시 면제. **싸라기눈 틱은 없음**(포챔스에서 제거됨). **방진(Overcoat) 특성·방진고글 도구는 포챔스에 없어서 무시.**
- **F-4 멸망의노래**(2차 지시 추가): 명중 시 장에 있는 양쪽에게 멸망 카운트 3. 매 턴 종료 시 카운트 로그(3→2→1) 후 감소, 0에서 다음 턴 종료에 HP 0 기절(건 턴 포함 4번째 = "3턴 후"). 방어·대타·황금몸 무시, **방음(Ability.blocksSound — 로스터 대상 없음)** 특성과 발동 시점 차징(공중날기 등) 포켓몬은 제외. 매직미러는 이미 `notReflectable`이라 반사 안 됨. 양쪽 동시 멸망 기절 시 **실효 스피드 느린 쪽 승리**. 배턴터치 카운트 이전은 교체 인프라(Phase 7.5) 대기.

#### G. 방어/판별 문구 재정의 (2차 지시 반영)

방어/판별/킹실드(`protectEffect === "block"`) 사용 시:
1. 연속 사용 스트릭 굴림 `(1/3)^streak`을 먼저 한다. 직전 실패했거나 이력이 없으면(streak 0) 확률 1 = **무조건 발동**.
2. **굴림 통과** → `[사용자]는 방어태세에 들어갔다!` (태세 진입). 그 뒤 상대가 이번 턴 쓴 기술이 —
   - 사용자를 겨냥했으면 → `[사용자]은(는) 공격으로부터 몸을 지켜냈다!` (실제로 막음)
   - 자기 대상 기술(칼춤·철벽 등)이면 → `[사용자]의 방어는 실패했다!` (막을 게 없음). activeProtect·streak는 정상 증가.
3. **굴림 실패**(연속 사용) → 태세 진입 없이 `[사용자]의 방어는 실패했다!`만.

Phase 6.5 §6-2 ⑦("막을 게 없으면 streak 0 리셋")은 이 지시로 대체 — 태세엔 들어가되 verdict만 실패.

#### H. 3차 플레이테스트 추가 발견 (2026-08-28)

전 그룹 완료 후 이어진 플레이로 추가된 항목. 각각 별도 커밋.

- **H-1 구애류 도구 발버둥**: `locksFirstMoveUsed`(구애스카프 등) 도구로 잠긴 기술의 PP가 0이 되면, 다른 기술에 PP가 남아 있어도 선택할 수 없어 턴 진행이 막혔다. → BattleLogPage `isStruggling(side)` 헬퍼 신설(`!hasUsableMove` OR 잠긴 기술 PP 0) → 선택 없이 `STRUGGLE_MOVE` 자동 진행. UI 배선만(엔진은 구애 잠금 미강제). 커밋 `b50bc32`.
- **H-2 대타 막힘 문구**: 변화기가 대타에 통째로 막혀 본체에 아무것도 못 했을 때(데미지 없음) → `ActionLogEntry.blockedBySubstituteMoveName` → `[상대]의 [기술명]은(는) 실패했다!`. 데미지 기술이 대타를 실제로 깎은 경우는 기존 `대타가 대신 맞았다!` 유지(2차 확인). 소리 기술은 원래대로 본체 통과. 커밋 `fe6c2da`·`57cd5c3`.
- **H-3 잠듦/얼음 해제 UI 순서·문구**: `selfCuredStatus`(자연 해제 + `thawsUserOnUse`)를 `curedStatus`에 머지하지 않고 `ActionLogEntry.selfWokeBeforeMove` 별도 축으로 분리 → 로그에서 **기술 줄보다 먼저** 렌더(움직이기 전 상태 판정 순서). `STATUS_CURE_TEXT.sleep`: `OO의 눈을 떴다!` → `OO은(는) 눈을 떴다!`(주격). 커밋 `fe6c2da`.
- **H-4 가루/포자 기술 풀타입 면역**: `MoveClassification`에 `"가루"` 추가, 수면가루·저리가루·독가루·목화포자·분노가루 5종에 태깅. `battleSimulator`의 `blockedByPowderImmunity`(`classification "가루"` && 방어측 풀타입) → `opponentEffectsBlocked`에 합류. 로그 `powderBlockedMoveName`. 방진 특성·방진고글 도구는 로스터에 없어 생략. 커밋 `b382725`.
- **H-5 씨뿌리기 풀타입 면역**: H-4와 같은 축의 본가 면역. `setsLeechSeed` 적용 전 `defender.types.includes("풀")` 확인 → `leechSeedBlockedByGrass`, 씨앗 미부착. 커밋 `967c910`.

### 1-4. 범위·리스크

- **표시층 + 로직 둘 다 건드린다**(당초 "문구만"에서 확장). C·D·G는 표시층 중심, E·F는 `battleSimulator` 로직 + 신규 `Move`/`Ability` 플래그.
- 조사(은/는·이/가·을/를·로/으로) 헬퍼(`eunNeun`·`iGa`·`eulReul`·`roEuro`)는 6.5 정비분 그대로 재사용.
- 신규 `Move` 플래그(`crashFraction`·`selfDamageFractionOnUse`·`setsTargetHpToUserHp`·`reversalPower`·`cancelsTargetCharge`·`counters`·`setsPerishSong`)와 `Ability` 플래그(`blocksSound`)는 해당 기술/특성에만 붙는 opt-in이라 기존 회귀 위험 낮음. 차징 1턴째 문구는 `Move` 필드 대신 `CHARGE_TURN_MESSAGE` id 매핑(BattleLogPage)으로 처리 — 로직 무변경.
- 착수 순서: **B → C/D/G(문구) → E(버그) → F(신규 기술) → A(빌더 UI)**. 커밋은 그룹 단위로 쪼갠다.

### 1-5. 진행 상황 (2026-08-28)

전 그룹 **완료**. 커밋: `6a8c655`(B·C·D·G) → `159d2d6`(방어 2차 지시·F-3) → `3f46e0f`(E·F-2) → `9d9e5ac`(A) → `d35c475`(F-1) → `a240dc0`(F-4) → `cbbedb3`(문서) → **H-1** `b50bc32` → **H-2/H-3** `fe6c2da`·`57cd5c3` → **H-4** `b382725` → **H-5** `967c910`. 각 항목 tsx 스모크 테스트 통과.

미착수(별도 논의/인프라 대기): 배턴터치 멸망 카운트 이전(7.5 교체 인프라), 사전 UI 힌트 `moveRestrictionMessage`의 "~해요" 말투(로그가 아니라 선택 경고라 유지).

---

## 2. 상대 진영 설치 기술 · 매직미러 (6.5 §8 이월)

**설치 기술 6종의 스택·제거·등장 데미지는 파티 선출·교체가 도입된 뒤에 구현**한다. 스텔스록만 6.5 §6-2 ④에서 부분 선반영됐다(항상 상대 진영, 설치 상태 + 환경 UI + 설치/실패 문구까지). 매직미러(Magic Bounce)는 2-5로 분리했고 메가픽시 로스터 추가와 함께 **이미 배선 완료**(2026-08-28) — 아래 참조.

### 2-1. 정의

변화 기술 중 상대방 진영에 부정적인 장치를 설치하는 기술("장판기"). 압정류는 중복 사용으로 효과가 누적된다.

**해당 기술 6종**: 끈적끈적네트 · 독압정 · 비검천중파 · 스텔스록 · 암석액스 · 압정뿌리기

### 2-2. 스택 규칙

- **독압정**: 1장 = 일반 독, 2장 = 맹독.
- **압정뿌리기**: 1장 = 최대 HP 1/16, 2장 = 1/8, 3장 = 1/4.
- 스텔스록·끈적끈적네트 등 나머지는 1장으로 고정(중복 무의미 → 재사용 시 "실패").

### 2-3. 제거 방법

- **고속스핀 / 안개제거**: 자기 진영(고속스핀) 또는 양 진영(안개제거)의 설치물을 걷어낸다.
- **독압정 한정**: 독 타입 포켓몬이 교체로 나오면 그 즉시 사라진다.

### 2-4. 등장 시 처리 (교체 인프라 선행)

- 교체로 포켓몬이 나오는 순간 그 진영의 설치물이 발동한다 — 스텔스록(바위 상성 배율만큼 HP), 압정뿌리기(스택별 비율), 독압정(독/맹독), 끈적끈적네트(스피드 -1), 비검천중파(고스트 부착) 등.
- 부유·비행 타입·에어벌룬 등 "땅에 닿지 않는" 대상은 압정뿌리기·끈적끈적네트·독압정을 무시한다(스텔스록은 그대로 맞음).
- **설치 시 문구(스텔스록, 6.5에서 선반영)**: `"우리 편 필드에 뾰족한 바위가 떠다니기 시작했다!"` / (상대 진영) `"상대 편 필드에 뾰족한 바위가 깔렸다!"`

### 2-5. 매직미러(Magic Bounce) — 구현 완료 (2026-08-28, 메가픽시 로스터 추가와 함께)

메가픽시(픽시 + 픽시나이트)가 이 특성을 고정으로 갖게 되면서, 교체 인프라를 기다리지 않고 **현행 1대1 엔진 기준으로 배선했다**. 반사 자체는 교체 없는 대면에서도 완전히 정의된다(도발·도깨비불·씨뿌리기·랭크 하락기 등이 시전자에게 그대로 꽂힘). 스텔스록 반사만 "등장 데미지가 없어" 진영 플래그만 뒤집히는 실질 무해 상태로 남는다.

**반사 대상 판정** (`battleSimulator.ts` `bouncedByMagicMirror`):
- `move.category === "status"` 이고
- `isOpponentTargetingMove(move)` 가 참이거나 `move.setsHazard` 가 있고 (설치기는 `isOpponentTargetingMove`가 잡지 않아 OR로 포함)
- `move.notReflectable` 가 아니고 (반사 제외: 고스트 저주·추억의선물·멸망의노래·흔들흔들댄스에 이 플래그를 붙였다)
- 방어측 `defenderAbility?.reflectsOpponentStatusMoves` 가 참이고
- 방어측의 방어/대타/황금몸으로 이미 막히지 않았을 때(`!opponentEffectsBlocked`).

**처리 방식**: "방어측 방향 효과" 블록(랭크변화 → 상태이상 → volatile → 씨뿌리기 → 사슬묶기/앙코르 → 스텔스록)에 진입하기 직전 `attacker`/`defender`(+ Ability·Item 바인딩)를 통째로 맞바꾸고, 블록이 끝나면 원위치한다. 그래서 되돌린 기술은 **원래 시전자 기준으로 타입 면역·조사·도구·승기·멘탈허브까지 전부 재평가**된다(예: 개굴닌자의 도깨비불은 시전자에게 반사돼 화상, 리자몽의 도깨비불은 시전자가 불꽃 타입이라 조용히 무산). 반사된 기술은 명중 굴림을 건너뛴다(빗나가지 않음).

**틀깨기 상호작용**: `MOLD_BREAKER_IMMUNE_ABILITY_NAMES`에서 `"매직미러"`를 제거했다(본가에서 틀깨기가 매직미러를 무시하는 게 맞다 — 기존 목록이 부정확했음). 이제 공격측이 틀깨기면 `resolveEffectiveDefenderAbility`가 `defenderAbility`를 `undefined`로 만들어 반사도 자연히 무산된다.

**로그(임시)**: 메인 라인에 `— OO의 매직미러! 기술이 되돌아왔다!` 클립을 붙이고, 상태이상 onset·하품·스텔스록 문구의 주어를 `bouncedMoveName` 유무로 뒤집는 최소 처리만 했다. 상대 랭크 하락 기술은 애초에 이 엔진에 전용 로그 줄이 없어(§1에서 신설 예정) 반사돼도 눈에 보이는 줄이 없다 — §1 문구 정비 때 매직미러 케이스를 함께 다룬다.

**아직 안 한 것**: 뽐내기/부추기기(랭크업+혼란)가 통째로 반사되는지는 `inflictsVolatile` + `statChanges` 둘 다 opponent라 자동으로 되지만, 로그 표기는 §1 대기. 바꿔치기·트릭·스킬스왑은 `isOpponentTargetingMove`가 false라 반사 대상에서 자연히 빠진다(별도 플래그 불필요).

### 2-6. 선행 조건

- **파티 선출·교체 인프라**. 아직 어느 페이즈에서 만들지 정해지지 않았다 — 정해지면 이 §의 착수 시점도 거기에 맞춘다.
- 교체가 생기면 6.5에서 "1대1·교체 없음"을 근거로 되돌려둔 것들도 같이 복원 대상이다:
  - 특성 가속(Speed Boost)의 "교체로 나온 턴엔 미발동" 예외 (6.5 §6-2 ①에서 `state.turnNumber > 1` 가드를 제거했음).
  - 일루전(조로아크)·일부 등장 효과 특성의 실제 로직.

---

## 3. 곁다리 특성 로직 — 로스터 확장과 함께 처리 중

§1·§2와 성격이 다른 "새 포켓몬을 넣다 보니 딸려온" 특성 배선을 여기 모은다.

### 3-1. 완료 (2026-08-28)

- **복안** (비비용, 6.5 §9) — 자기 기술 명중률 ×1.3. `Ability.userAccuracyMultiplier` → `battleSimulator`의 `accuracyExtraMultiplier`에 합류(모래숨기와 같은 축, 방향만 반대). 필중기·100% 기술엔 무영향.
- **페어리오라** (메가플라엣테, §9와 별개로 플라엣테 로스터 추가분) — 장 전체 페어리 기술 위력 ×1.33. `Ability.auraMoveTypeMultiplier` → `moveContext.resolveMoveContext`가 공격측·방어측 양쪽을 보고 offense 배율에 합류(배틀 시뮬 + 매치업 페이지 공용). 오라브레이크는 로스터에 대상 없어 미구현.
- **매직가드** (픽시) — 공격기 데미지 외 부가 피해 무효. 이 엔진에 실재하는 5경로만 게이팅: 상태이상 지속 데미지(걸린 상태·맹독 카운터는 유지, HP만 0)·씨뿌리기(상대 회복도 없음)·반동기·생명의구슬 반동·까칠한피부류 접촉 반사. `Ability.negatesIndirectDamage`. 혼란 자멸·화상 물리 감소는 막지 않음(본가 일치). 날씨 스립·압정·소금절이·포박기 2차 데미지·스텔스록 등장 데미지는 이 엔진에 아직 없어 대상 없음(생기면 같은 플래그로 확장 — 타입 주석 명시).
- **매직미러** (메가픽시) — §2-5 참조.
- **인분** (비비용) — 방진(Shield Dust)형. 방어측이 이 특성이면 데미지 기술이 자신에게 딸려 거는 **추가효과**를 전부 무산. `Ability.blocksSecondaryEffects` → `battleSimulator`의 4곳(statChanges 필터 · inflictsStatus 블록 · inflictsVolatile per-entry · 왕의징표석 풀죽음)에서 게이팅. 로그 `secondaryBlockedByAbilityName` → "OO의 인분! 추가 효과를 받지 않는다!".
  - **"추가효과" 정의 확정** (§ 별도 논의): 데미지 기술(`category !== "status"`)이 상대에게 딸려 거는 상태이상·행동방해·랭크변화. **`chance` 유무 무관** — 연옥 100% 화상·일렉트릭네트 100% 스피드↓·천추지한 100% 특공↓도 추가효과. 변화기 주효과(도깨비불 등)·자기 대상 효과(반동·자기 랭크변화)는 제외. 왕의징표석(도구발 풀죽음)도 인분이 막는다.
  - 계획 초안에서 `Move`에 `secondary?` 플래그 + 5개 기술 태깅을 예상했으나, 정의가 "데미지기 + 상대 방향"이라는 **순수 구조 규칙**으로 정리되면서 **스키마 변경·데이터 마이그레이션 불필요**해졌다. `hasSheerForceSecondaryEffect`(우격다짐)와 같은 정의를 공유(우격다짐은 여기에 자기 랭크업까지 얹음) — 주석에 단일 기준 명시.
  - **곁다리 버그 수정**: 확률부 `statChanges`(불꽃춤 자기 특공↑ 50% · 브레이크클로 상대 방어↓ 50%)가 `applyMoveStatChanges` 기본 `includeChanceBased=true` 때문에 굴림 없이 100%로 적용되던 걸, battleSimulator에서 명시적으로 굴리도록 수정.

### 3-2. Phase 7.5로 이월

- **플라워베일 · 공생** (플라엣테) — 현행 1대1 엔진에서 구조적으로 발동 불가(플라워베일은 아군 풀타입 보호가 본질이고 플라엣테 자신은 페어리라 무효, 공생은 아군 슬롯 필요). 파티 선출·교체 인프라 선행이라 **[Phase 7.5](phase7.5-plan.md) §3**으로 옮겼다. `abilities.json`에 설명 스텁만 남아 있다.
