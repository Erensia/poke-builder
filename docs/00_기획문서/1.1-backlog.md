# ver.1.1 백로그

## 0. 이 문서의 성격

`post-1.0-backlog.md`(§1~§7)의 1.0.x 개선 항목을 소진한 뒤 **아직 코드에 반영되지 않은 이월 항목**과,
그 위에 새로 접수된 **UI 개선 요청**을 모아 ver.1.1 업데이트로 처리한다.

- **post-1.0에서 이월(코드 미반영):** `post-1.0-backlog.md` §2 "포켓몬 프로필 이미지" 1건.
  나머지 §1·§3·§4(+후속)·§5·§6·§7은 전부 머지 완료(2026-09-04, 최신 커밋 `5d61fbd` 기준).
  → 이 문서 **§1**로 이월.
- **신규 UI 개선 요청(2026-09-07~08):** 타입 상성표 종합 행(2-1) · 배틀타워 VS 클릭 시작(2-2) ·
  전체 모바일 대응(2-3) · 배틀 로그 변환자재 문구 분리(2-4) · 연타 로그 타별 분리(2-5) ·
  타입 상성표 색상·디자인 정리(2-6) · 모바일 폴리시 잔가지(2-7) · 속이기 등장턴 판정 버그(2-8) ·
  변환자재 등장당 1회(2-9) · 연타 방어측 on-hit 특성 타별 로그(2-10) · 변환자재 빗나감 문구 누락(2-11) ·
  메가진화 로그 폼 이름(2-12) · 로그 조사 자동 판별(2-13). → 이 문서 **§2**.
- **처리 순서:** §2-4~§2-5·§2-8~§2-13(PR #27) → §2-6 → §2-7 → **§1(이미지)은 항상 맨 마지막.**

### 진행 상황 (2026-09-08 갱신)

| 항목 | 상태 |
|---|---|
| §2-1 타입 상성표 종합 행 | ✅ PR #19 머지 · 배경색 후속 PR #21 |
| §2-2 배틀타워 VS 클릭 시작 | ✅ PR #20 머지 · 사용자 확인 완료 |
| §2-3 전체 모바일 대응 | ✅ PR #22·#23·#24·#25·#26 머지 · 모바일 확인 완료 |
| §2-4 변환자재 문구 분리 | 🔻 PR #27 (리뷰 대기) |
| §2-5 연타 로그 → 타별 분리 | 🔻 PR #27 (리뷰 대기) — 개정: 타수별 한 줄씩 |
| §2-8 속이기 등장턴 판정 버그 | 🔻 PR #27 (리뷰 대기) |
| §2-9 변환자재 등장당 1회 | 🔻 PR #27 (리뷰 대기) |
| §2-10 연타 방어측 on-hit 특성 타별 로그 | 🔻 PR #27 (리뷰 대기) |
| §2-11 변환자재 기술이 빗나가면 문구 누락 | 🔻 PR #27 (리뷰 대기) |
| §2-12 메가진화 로그 폼 이름 (메가이어롭) | 🔻 PR #27 (리뷰 대기) |
| §2-13 로그 조사 하드코딩 `은(는)` → 자동 판별 | 🔻 PR #27 (리뷰 대기) |
| §2-6 타입 상성표 색상·디자인 정리 | 대기 (§1 이미지 앞) |
| §2-7 모바일 폴리시 잔가지 | 대기 (§1 이미지 앞) |
| §1 포켓몬 이미지 | 대기 (자료 조사 필요, 1.1 **맨 마지막**) |

---

## 1. 포켓몬 프로필 — 이니셜 뱃지 → 실제 포켓몬 이미지  *(post-1.0 §2에서 이월)*

> 원문 전체는 `post-1.0-backlog.md` §2 참조. 아래는 요약 + 미결정 사항.

### 1-1. 현재 방식

포켓몬 아바타는 전부 **이름 첫 글자 + 타입 색 그라디언트**로 그려진다.
`pokemon.name.at(0)` + `linear-gradient(135deg, TYPE_COLORS[type0], TYPE_COLORS[type1 ?? type0])`.

등장 위치 5곳:

| 위치 | 클래스 | 크기 | 모양 |
|---|---|---|---|
| 파티 빌더 슬롯 (`PartySlotCard.tsx`) | `.party-slot-avatar` | 36×36 | radius 10px |
| 배틀 셋업 카드 (`BattleSetupCard.tsx`) | `.party-slot-avatar` 재사용 | 36×36 | radius 10px |
| 매치업 슬롯 (`MatchupSlotCard.tsx`) | `.matchup-slot-avatar` | 42×42 | radius 11px |
| 도감 리스트 (`PokedexPage.tsx`) | `.pokedex-list-avatar` | 28×28 | 원형 |
| 도감 상세 (`PokedexPage.tsx`) | `.pokedex-detail-avatar` | 56×56 | 원형 |

### 1-2. 목표

각 크기에 맞는 포켓몬 이미지로 교체. 이미지가 없거나 로드 실패 시 **현재 이니셜+그라디언트로 폴백**.

### 1-3. 잠정 방향 (post-1.0 메모에서 제안, 미확정)

- `pokemon.json`에 `dexNo`(전국도감 번호) 필드 추가 → 로스터 병합 스크립트에 `dexNo` 채우는 단계 추가.
- 외부 CDN 참조(PokéAPI 스프라이트 / Pokémon HOME 렌더) + 이니셜 뱃지 폴백.
- 공유 `<PokemonAvatar pokemonId size formLabel />` 컴포넌트로 5곳 통합
  (현재 `avatarGradient` 계산이 컴포넌트마다 중복).
- `<img loading="lazy" onError={폴백}>`, `object-fit: contain`, 배경에 기존 그라디언트 옅게.

### 1-4. 결정해야 할 것

- **이미지 소스 & 키잉**: (a) `dexNo` 필드 + CDN 참조 / (b) `public/sprites/` 번들 / (c) 외부 CDN
  직접 참조. 오프라인·속도·안정성 트레이드오프.
- **스프라이트 스타일**: 28px는 박스 아이콘, 36~56px는 gen5 도트 / Showdown / HOME 렌더 중 택.
  픽셀 아트면 작은 크기에서 `image-rendering: pixelated`.
- **메가 폼**: `megaEvolutions[].form`(예: `"리자몽-메가X"`)도 별도 이미지 매칭, 없으면 기본 폼 폴백.
- **라이선스**: 포켓몬 스프라이트/아트워크는 저작권물. 개인·비영리 토이 프로젝트 범위 확인.
- 로스터 현황: 약 228마리, `dexNo` 필드 없음, 세대별 확장 중.

관련: `docs/00_기획문서/engine-smoke-test-via-vite-page` 방식으로 렌더 검증.

---

## 2. UI 개선 (2026-09-07 접수)

### 2-1. 파티 빌더 타입 상성표 — 포켓몬별 약점 요약 "종합" 행 추가

> **✅ 구현 (2026-09-07, PR #19 dev 머지).** 표 `<tfoot>`에 종합 행 추가.
> - 헤더 이름 구분: 오른쪽 열 `종합` → **`종합(상성별)`**, 마지막 행 라벨 **`종합(포켓몬별)`**.
> - `종합(포켓몬별)` 셀 = **그 포켓몬의 약점(×2·×4) 타입 수**(숫자만). `×4`도 타입 1개로 집계,
>   `¼`·면역 등 저항은 세지 않음. 취약/보통/강점 판정은 쓰지 않음(그 규칙을 세로로 재사용하면
>   대부분 "강점"으로 쏠려 무의미했음). 검증: 이어롭-메가 = 격투·비행·에스퍼·페어리 → `4`.
> - `computePartyDefenseMatrix`가 `memberWeakCounts: number[]` 추가 반환. 좌하단 코너 셀은 공란.
> - **후속 (2026-09-07, PR #21):** 종합(포켓몬별) 행 배경을 헤더와 동일한 `--surface-2`로 통일
>   (숫자 셀만 흰색으로 튀던 문제). 그 외 표 색상·디자인 정리는 별도 후속으로 미룸(사용자 결정).

#### 현재 방식

`TypeCoverageSummary` = **포켓몬(열) × 공격타입 18종(행)** 매트릭스. 셀은 방어 배율(`½`/`¼`/`×2`/`×4`/
`면역`, 등배는 공란). 오른쪽 끝 **"종합" 열**이 그 타입 행에 대해 파티 전체가 `취약`/`보통`/`강점`
중 무엇인지 표기한다. 판정 규칙(`post-1.0` §1-3, 2026-09-02 확정): 그 행에서 약점 인원 `w`, 저항
인원 `r`(면역 포함)일 때 `강점 = r≥w ∧ r≥2` / `취약 = w−r≥2` / 그 외 `보통`.

즉 **가로 방향(타입별 파티 유불리)만 요약되고, 세로 방향(포켓몬 한 마리의 약점 분포)은 요약이 없다.**

#### 기대

표의 **가장 마지막 행**에도 "종합" 요약을 추가한다. 이 행의 각 셀은 **그 열 포켓몬 한 마리의 방어
상성 종합** — 약점이 얼마나 많은지 / 얼마나 단단한지를 한눈에 보여준다. 목적은 "타입별 파티
유불리"뿐 아니라 **개별 포켓몬의 약점 상성 파악**.

#### 대상 코드

- `src/components/TypeCoverageSummary.tsx` — `<tbody>` 마지막에 요약 행, 헤더 `<th>종합</th>` 대응.
- `src/components/TypeCoverageSummary.css` — 마지막 행 강조 스타일(`tbody tr:last-child` 규칙 이미 있음, 조정 필요).
- `src/lib/partyAnalysis.ts` — `computePartyDefenseMatrix`가 열별(포켓몬별) 요약도 계산해 반환
  (`PartyDefenseMatrix`에 `memberVerdicts: MemberDefenseSummary[]` 추가).

#### 설계 논점 / 결정해야 할 것

- **셀에 무엇을 표기?** 후보:
  - (a) 약점 수만 (`약점 3`) — 가장 단순.
  - (b) 약점 수 / 저항 수 (`3약 · 4저`).
  - (c) 열 verdict 라벨 (`무름` / `보통` / `단단` 같은 3단계) + 색상(기존 `tm-verdict-*` 재사용).
  - (d) (b)+(c) 조합.
- **열 verdict 규칙**: 행 규칙(`r≥w ∧ r≥2` / `w−r≥2`)을 세로로 그대로 재사용할지, 한 마리
  기준에 맞는 별도 임계값을 둘지(한 마리는 표본이 18타입으로 크므로 임계가 달라야 자연스러움).
  예: `×4` 존재 시 가중, 약점 4개 이상이면 `무름` 등. → **규칙 확정 필요.**
- **좌하단 코너 셀**(행 헤더 열 × 마지막 행 교차): 빈 칸 vs `"종합"` 라벨.
- **약점 타입 나열**: 셀 hover 툴팁으로 그 포켓몬의 `×2`/`×4` 타입 목록을 보여줄지.
- **정렬 없음** 원칙 유지(`post-1.0` §1-5) — 요약 행은 항상 맨 아래 1행 고정.

---

### 2-2. 배틀타워 셋업 — "대전 시작" 버튼 제거, VS 클릭으로 배틀 시작

> **✅ 구현 (2026-09-07, PR #20 dev 머지).** `.battle-setup-vs`를 `<button>`으로 —
> `onClick={handleProceed}` · `disabled={!canProceed}`. 셋업 화면의 별도 `.battle-start-button`
> 제거(선출 화면 쪽은 유지). "다음 (선출)"/"대전 시작" 구분은 `aria-label`·`title`로만,
> 비활성 시 `"양쪽 파티를 먼저 완성하세요"`. `:hover` 그라디언트 채움 / `:active` 축소 /
> `:disabled` opacity 0.4, skew 유지. **사용자 확인 완료, 지적 없음.**

#### 현재 방식

`BattleLogPage`의 셋업 화면 `.battle-setup-center`에 두 요소가 세로로 있다:

1. `.battle-setup-vs` — `<div aria-hidden="true">VS</div>` (레터마크, `skewX(-8deg)`, 클릭 불가).
2. `.battle-start-button` — 그 아래 별도 버튼. 라벨은 선출 필요 여부에 따라
   `"다음 (선출)"` 또는 `"대전 시작"`, `disabled = !canProceed`, `onClick = handleProceed`.

(코드: `BattleLogPage.tsx` 약 583~597행 / `BattleLogPage.css` 약 70~108행.)

#### 기대

**`.battle-start-button`을 삭제하고, VS 레터마크 자체를 누르면 배틀이 시작(=`handleProceed`)되도록** 변경.

#### 세부 / 결정해야 할 것

- `.battle-setup-vs`를 `<div aria-hidden>` → `<button type="button">`로 교체. `aria-hidden` 제거,
  접근성 이름 제공(`aria-label`), `cursor: pointer`, `:disabled` 스타일, 포커스 링(skew 변형과
  포커스 아웃라인 겹침 확인).
- **`disabled = !canProceed`** 유지 — 양측 빌드가 유효하지 않으면 못 누름.
- **이원 라벨 처리**: 지금은 버튼 텍스트로 `"다음 (선출)"` / `"대전 시작"`을 구분했다. VS만 남으면
  그 정보가 사라짐. 후보:
  - VS 아래에 작은 캡션 텍스트(`다음: 선출` / `대전 시작`)를 두고 클릭 대상은 VS.
  - VS의 `aria-label` / `title`에만 반영하고 시각적으로는 VS만.
  - VS 밑에 상태 문구(`양쪽 파티를 완성하세요` 등 disabled 사유)까지 노출.
  → **표시 방식 확정 필요.**
- **선출 화면(`.battle-select-actions`)의 "대전 시작" 버튼**은 이번 요청 범위 밖(셋업 화면 한정으로
  해석)이라 **유지**. 일관성을 위해 그쪽도 바꿀지는 별도 확인.
- GIF/스크린샷으로 클릭 → 배틀 진입 흐름 확인.

---

### 2-3. 전체 UI 모바일 친화적 조정

> **✅ 완료 (2026-09-07~08, PR #22~#26).** 공통 브레이크포인트 **760px**로 통일.
> - **#22** 셸/사이드바: ≤760px 사이드바를 햄버거 오버레이 드로어로 (`Sidebar.tsx` `mobileOpen`
>   state, 좌상단 고정 토글, 백드롭/Esc/내비클릭 닫힘, body 스크롤 잠금). `.app-main` 모바일
>   `margin-left:0` + 상단 패딩.
> - **#24** iOS 입력 확대 방지: ≤760px `input/select/textarea { font-size:16px !important }`.
> - **#23** 파티 빌더: 그리드 2열→1열(520→760), 헤더 세로 스택.
> - **#25** 배틀타워: 브레이크포인트 860→760, 셋업 보드 세로 스택(VS 맨 아래), 배틀 보드 1열.
> - **#26** 모달 하단 시트화(전체 폭·상단만 라운드·92vh, 공유 Modal 일괄) + 매치업 슬롯
>   280px→100%/max320. 도감 3종은 이미 760 대응돼 있어 무변경.
> - **남은 폴리시는 §2-7로 분리** (도감 sticky 헤더 vs 고정 토글, 드로어 포커스 트랩 등).

> **확정 (2026-09-07):** 사이드바 모바일 형태 = **햄버거 오버레이**(상단 좌측 햄버거 → 탭 시
> 사이드바가 오버레이로 슬라이드인, 기존 항목 구조 재사용). 공통 브레이크포인트 = **760px**
> (기존 다수와 일치, 배틀 셋업 보드 등 넓은 레이아웃만 필요 시 별도 규칙). 범위가 크므로
> 페이지 단위로 쪼개 여러 PR 가능.

#### 현재 상태 (진단)

- `index.html`에 `viewport` meta는 있음.
- `App.css` `.app-main`: `margin-left: 64px`(사이드바 레일 폭) **고정**, `padding: 2rem clamp(1.25rem, 4vw, 3rem)`,
  `justify-content: center`. **미디어쿼리 없음.**
- `Sidebar`: 폭 `64px` 레일 → hover 시 `220px` 확장. **터치 환경에서 hover 확장은 부적합.**
- 미디어쿼리가 있는 CSS 파일이 **산발적이고 브레이크포인트가 제각각**:
  `BattleLogPage.css`(860px) · `MatchupPage`·`MoveDexPage`·`ItemDexPage`·`PokedexPage`(760px) ·
  `PartyBoard.css`(520px). 공통 기준 없음.
- 표가 여럿(타입 매트릭스 18행, 도감 리스트, 배틀 로그, 매치업) — 좁은 폭에서 가로 스크롤/넘침.
- 모달(`Modal.tsx`)의 모바일 시트화 여부 미확인.

#### 기대

패딩·여백·레이아웃을 **모바일 디바이스에서도 확인 가능하도록 전체적으로 조정**.

#### 작업 후보

- **공통 브레이크포인트 정하기** (예: `640px` 모바일 / `900px` 태블릿) 후 기존 760/860/520을 정리.
- **`.app-main`**: 모바일에서 `margin-left` 제거, `padding` 축소(예: `1rem 0.75rem`).
- **`Sidebar`**: 터치용 대체 — 하단 탭바 or 햄버거 오버레이 or 탭 토글. (hover 확장 폐지 또는 병행)
- **각 페이지 그리드 → 1열 스택**: `PartyBoard`, `MatchupPage`, 배틀 셋업(양측 12카드 + 중앙 VS),
  도감 리스트/상세 2단.
- **카드 패딩 `clamp()` 화**, 폰트 크기 하한.
- **표**: sticky 헤더 유지 + 가로 스크롤 힌트(그림자/페이드), 또는 카드형 재배치.
- **모달**: 좁은 폭에서 전체화면 시트(하단에서 슬라이드) 스타일.
- **터치 타겟 최소 ~44px** — 슬롯 카드의 작은 아이콘 버튼들 점검.
- 실제 기기 폭(360 / 390 / 414 CSS px)에서 육안 확인.

#### 대상 코드

- `src/App.css`, `src/index.css`, `src/components/Sidebar.css`
- 각 페이지 CSS: `PartyBoard.css`, `BattleLogPage.css`, `MatchupPage.css`, `PokedexPage.css`,
  `MoveDexPage.css`, `ItemDexPage.css`, `Modal.css`, `MatchupSlotCard.css`, `PartySlotCard.css`
- `TypeCoverageSummary.css`(2-1과 함께)

#### 결정해야 할 것

- (사이드바 형태·브레이크포인트는 위 "확정" 박스 참조)
- 페이지 분할 순서 — 예: 셸/사이드바 → 파티 → 배틀타워 → 도감.

---

### 2-4. 배틀 로그 — 변환자재/리베로 타입 변경 문구를 별도 줄로

#### 현재 방식

`BattleLogPage.tsx`의 액션 로그 메인 줄(`.battle-turn-line`, ≈1365행)에서 데미지·다단히트
표기 **뒤에 `·`로 이어붙여** 인라인 출력한다:

```
마스카나의 트리플악셀 — 87 데미지 (53.7%) · 3타 명중 · 변환자재 발동! 타입이 얼음으로 바뀌었다!
```

(`action.changedOwnTypeAbilityName` / `action.changedOwnTypeTo` / `roEuro` 사용.)

#### 기대

데미지 줄과 분리해 **자체 줄**로:

```
마스카나의 변환자재!
마스카나는 얼음타입이 되었다!
```

- 급소(`급소에 맞았다!`)·상성 문구가 이미 메인 줄 아래 별도 `.battle-turn-line`으로 빠져 있으니
  같은 패턴을 따른다(≈1380행대).
- 문구도 `~ 발동! 타입이 ~로 바뀌었다!` → 2줄(`~의 변환자재!` + `~는 ~타입이 되었다!`)로 교체.
  조사 처리는 기존 `eunNeun` 헬퍼 재사용.

#### 대상 코드

- `src/components/BattleLogPage.tsx` — `action.changedOwnTypeTo` 인라인 렌더(≈1365~1371행)를
  메인 줄에서 빼서 별도 줄 블록으로.

---

### 2-5. 배틀 로그 — 연타(멀티히트) 로그를 타수별로 분리 (개정 2026-09-08)

> **개정:** 처음엔 "합산 데미지 한 줄 + `N번 맞았다!` 한 줄"로 넣었으나(1차),
> 본가처럼 **타마다 그 타의 데미지로 한 줄씩** 찍는 방식으로 재수정.

#### 기대 형식

- 타마다 `(사용포켓몬)의 (기술) — (그 타 데미지) 데미지 ((그 타 퍼센트)%)` 한 줄. 퍼센트는
  **그 한 타** 데미지 ÷ 대상 최대 HP(누적 아님).
- **1타 직후**에만 변환자재 2줄(§2-4 — 첫 기술에서 타입 변경).
- 급소는 **타마다 독립 판정** → 급소 난 그 타 줄 바로 뒤에 `급소에 맞았다!`(1타면 변환자재 줄 다음).
- 모든 타 끝나면 `N번 맞았다!` → 그 뒤 `효과가 굉장했다!` 등 상성 문구.
- 단타 기술은 기존 그대로(메인 줄 + 단타용 `급소에 맞았다!`).

예 — 3타 전부 명중:
```
마스카나의 트리플악셀 — 85 데미지 (n%)
마스카나의 변환자재!
마스카나는 얼음타입이 되었다!
마스카나의 트리플악셀 — 85 데미지 (n%)
마스카나의 트리플악셀 — 85 데미지 (n%)
3번 맞았다!
효과가 굉장했다!
```
예 — 2타 명중, 1타에 급소:
```
마스카나의 트리플악셀 — 85 데미지 (n%)
마스카나의 변환자재!
마스카나는 얼음타입이 되었다!
급소에 맞았다!
마스카나의 트리플악셀 — 85 데미지 (n%)
2번 맞았다!
```

#### 구현 (PR #27)

- **엔진**: `ActionLogEntry.hits?: { damage, damagePercent, critical }[]` 신설.
  `battleSimulator.ts` 다단히트 루프에서 타마다 push. `damagePercent`는 그 타 ÷ 대상 최대 HP.
  총 `damage`/`hitCount`/`critical`은 그대로 둠(다른 소비처 호환).
- **UI**: `action.hits`가 있으면 메인 줄엔 1타 몫만(`headDamage`), 그 아래 §2-5 블록이 1타 급소 →
  2타부터 타별 줄(+급소) → `N번 맞았다!`. 변환자재 2줄(§2-4 블록)은 메인 줄 직후라 자동으로 1타 뒤.

---

### 2-6. 타입 상성표 — 색상·디자인 정리 (2026-09-08 추가)

§2-1로 `종합(포켓몬별)` 행이 붙으면서 기능은 완성됐으나, 표 전체의 색·간격·구분선을 한 번
다듬기로 미뤄뒀다(§2-1 완료 노트 "그 외 표 색상·디자인 정리는 별도 후속으로 미룸").

#### 현재 상태 / 손볼 지점 (착수 시 재확인)

- **배율 셀 색상**: `cnt-quad`/`cnt-weak`/`cnt-resist`/`cnt-quadresist`/`cnt-immune` — `--danger*` /
  `--safe*` 톤 재사용. `×4`(danger-strong 배경 흰 글자)와 `½`(danger-bg 옅은 배경)의 대비가
  들쭉날쭉, 다크 모드에서 특히 확인 필요.
- **종합(상성별) 열 vs 종합(포켓몬별) 행**: 둘 다 "종합"인데 시각적 위계가 약함. 행/열 교차
  코너 셀 처리도 밋밋(현재 공란 + `--surface-2`).
- **행 헤더(타입 배지)**: `type-coverage-badge` 고정 색, 좁은 폭에서 셀과 간격 빠듯.
- **가로 스크롤**: `.type-matrix-scroll`가 스크롤되지만 힌트(끝단 페이드/그림자)가 없어 더 있는 걸
  모를 수 있음. 행 헤더 `position: sticky; left: 0` 고정도 검토(스크롤 시 타입 이름 유지).
- 전반적으로 셀 `padding`·`font-size`가 작아 밀도가 높음 — 가독성 vs 한 화면에 담기 트레이드오프.

#### 대상 코드

- `src/components/TypeCoverageSummary.tsx` / `TypeCoverageSummary.css`
- 색 토큰은 `src/index.css`

#### 결정해야 할 것

- 배율 색 스케일을 유지할지, `cnt-*`를 별도 팔레트로 뺄지.
- 두 "종합"의 라벨/색을 어떻게 구분할지.
- 행 헤더 sticky-left 도입 여부(데스크톱에도 적용되는 변경).

---

### 2-7. 모바일 폴리시 잔가지 (2026-09-08 추가)

§2-3 진행 중 PR 설명에만 적고 항목화하지 않은 마감 수준 항목들.

- **도감 sticky 헤더 ↔ 고정 햄버거 토글 겹침**: `≤760px`에서 햄버거 토글이 `position: fixed;
  top: 0.7rem` (z-index 42). `MoveDexPage`의 `.movedex-head-row`(`sticky; top: 0`), 그리고
  `PokedexPage`/`ItemDexPage`의 목록 패널·검색창이 스크롤되면 토글 버튼 아래로 파고든다.
  → 모바일에서 해당 sticky 요소들의 `top`을 토글 높이만큼(≈3.5rem) 내리거나, 셸에 얇은
  상단 바를 두고 그 아래에 붙이기.
- **드로어 포커스 관리**: 사이드바 드로어가 열릴 때 포커스가 안으로 이동/트랩되지 않고, 닫힐 때
  토글 버튼으로 복귀하지 않는다. 키보드/스크린리더 사용자 대상 폴리시.
- **터치 타깃**: 파티 슬롯 카드의 `✕`(슬롯 비우기)·`💾`(샘플 저장) 코너 버튼이 20×20px.
  모바일에서 28px 정도로 키우되 이름 텍스트와 겹치지 않게(§2-3 B에서 보류한 항목).
- **실기기 육안 점검**: 360 / 390 / 414 CSS px에서 각 페이지 한 번씩.

#### 대상 코드

- `src/components/Sidebar.tsx` / `Sidebar.css` (포커스 트랩)
- `src/components/MoveDexPage.css` · `PokedexPage.css` · `ItemDexPage.css` (sticky `top`)
- `src/components/PartySlotCard.css` (코너 버튼 타깃)

---

### 2-8. 속이기(Fake Out) — 교체·비리드 등장 시 실패 버그 (2026-09-08)

> **✅ 구현 (PR #27).**

#### 버그

`battleSimulator.ts`의 `first-turn-only` 판정이 **배틀 전체의 턴 번호(`state.turnNumber !== 1`)**
로 돼 있었다(1v1·교체 없던 구버전 잔재). 그래서 교체·유턴으로 3턴째 나온 포켓몬이 속이기를
쓰면 실패했다.

#### 규칙 / 수정

속이기는 **그 포켓몬이 등장한 뒤 처음으로 행동을 개시하는 턴**에만 성공(리드의 1턴, 자발적
교체·기절 교체·유턴 등으로 나온 뒤 첫 행동 턴).

- `BattleFighterState.hasActedSinceSwitchIn` 신설. `performSwitch`에서 새로 나온 포켓몬 리셋
  (배틀 시작 리드는 undefined = 아직 안 함).
- `resolveAction` 진입 후 `first-turn-only` 체크는 `!attacker.hasActedSinceSwitchIn`일 때만 통과,
  그 직후 `= true`로 소진. **행동이 막혀도(마비·풀죽음 등) 소진**(본가 동일). 유턴 등으로 턴
  중 들어와 그 턴에 `resolveAction`을 안 거치면 다음 턴까지 false → 다음 턴 성공(본가 동일).
- 검증(엔진 스모크): 리드 이어롭 1턴 속이기 성공 / 2턴 `usageCondition` / 마스카나→이어롭 교체
  후 이어롭 첫 턴 속이기 성공 / 그 다음 턴 `usageCondition`.

---

### 2-9. 변환자재/리베로 — 등장당 1회 발동 (2026-09-08)

> **✅ 구현 (PR #27).**

#### 버그

`changesUserTypeToMoveType` 처리가 **매 턴 기술을 쓸 때마다** 타입을 그 기술 타입으로
바꿨다. 본가 9세대는 **필드 등장당 1회**만 발동한다.

#### 규칙 / 수정

등장 후 첫 기술에서만 타입 변경 발동. 이후엔 다른 타입 기술을 써도 안 바뀜(로그도 안 뜸).
교체로 물러났다 다시 나오면 다시 1회 가능. 이미 바뀐 타입이 유지되는 것은 그대로.

- `BattleFighterState.proteanActivatedSinceSwitchIn` 신설. `performSwitch`에서 리셋.
- 변환자재 블록 조건에 `&& !attacker.proteanActivatedSinceSwitchIn` 추가, 발동 시 `= true`.
  이 블록은 상태이상·행동방해 차단 이후 지점이라 **기술을 실제로 못 쓰면 소진 안 됨**(본가
  동일 — 속이기와 반대). 빗나감은 발동(기술을 쓴 것).
- 검증(엔진 스모크): 마스카나 1턴 트리플악셀(얼음)→타입 얼음·`changedOwnTypeTo="얼음"` /
  2턴 번개펀치(전기)→`changedOwnTypeTo` 없음·타입 얼음 유지.

---

### 2-10. 연타(멀티히트) — 방어측 on-hit 특성을 타별 로그로 (2026-09-08)

> **✅ 구현 (PR #27).**

지구력·깨어진갑옷 같은 피격 시 발동 특성은 엔진상 이미 타마다 발동하는데(지구력=+1 방어 3회
= +3), **로그만 합산**돼 한 줄로만 나왔다. 이제 타마다 그 타의 특성 반응을 그 타 데미지 줄
바로 뒤에 찍는다.

- **엔진**: `HitAbilityEvent`(타별 `abilityName`·랭크 변화·반격 데미지·상태이상·헤롱헤롱·기술 봉인·
  도구 강탈·미라·떠도는영혼·모래뿜기) 신설. `triggerAbilityHitEffect`가 이번 호출에서 한 일을
  `ev`로도 담아 반환 → `ActionLogEntry.hits[i].abilityEvent`. 기존 집계 필드는 그대로 두고
  병렬로만 담는다(단타 경로 무변경).
- **UI**: `hitAbilityEventLines` 헬퍼로 타별 렌더. `action.hits`가 있으면 기존 집계 렌더 줄
  (정전기·헤롱헤롱바디·까칠한피부·지구력/깨어진갑옷·미끈미끈·떠도는영혼·모래뿜기·미라 인라인)은
  전부 `!action.hits`로 숨긴다.
- **표기 정책**: 랭크류·반격뎀은 매 타 / 정전기·불꽃몸·헤롱헤롱바디·저주받은바디 등 1회성은
  실제 발동한 그 타에 한 번 / 나쁜손버릇·미라는 첫 타에 한 번.
- 검증(엔진 스모크): 트리플악셀 vs 까칠한피부(타별 18 반격) · vs 깨어진갑옷(타별 방어−1/스피드+2)
  · vs 지구력(타별 방어+1) · vs 정전기(1타에만 마비, 2·3타 이벤트 없음).
- **범위 밖**: 부자유친(캥카 메가 2타)은 `minHits` 경로가 아니라 그대로 합산 한 줄.

---

### 2-11. 변환자재 기술이 빗나가면 발동 문구 누락 (2026-09-08)

> **✅ 구현 (PR #27).**

`resolveAction`의 `if (!hit)` 조기 반환 객체에 `changedOwnTypeTo`/`changedOwnTypeAbilityName`이
빠져 있어, 변환자재로 타입은 바뀌었는데(명중 굴림 전 발동) 빗나가면 로그에 `~의 변환자재! /
~는 …타입이 되었다!`가 안 떴다. 조기 반환 객체에 두 필드 추가.

- 검증(엔진 스모크): 마스카나 리프스톰(풀) 강제 빗나감 → `hit:false`·`changedOwnTypeTo:"풀"`·
  타입 풀로 변경 확인.

---

### 2-12. 메가진화 로그 — 폼 이름을 정식 명칭으로 (2026-09-08)

메가진화 로그가 데이터상 폼 이름을 그대로 써서 `이어롭은 이어롭-메가로 메가진화했다!`로
나왔다. 정식 명칭은 `이어롭은 메가이어롭으로 메가진화했다!`.

- `pokemonForm.ts`에 `megaFormFullName(mega)` 신설 — `"{base}-메가{X|Y|Z}?"` → `"메가{base}{X|Y|Z}?"`.
  예상 밖 형식이면 폼 이름 그대로.
- `battleSimulator.ts` `applyMegaEvolution` 로그를 이 이름으로, 조사(`로`/`으로`)도 바뀐 이름 기준.
- 검증: 빌드 통과. (라이브 로그 육안 확인은 세션 브라우저 제약으로 미완 — 스톤 든 이어롭 메가진화 시)

---

### 2-13. 배틀 로그 — 하드코딩된 조사 `은(는)`·`이(가)` 등을 자동 판별로 (2026-09-08)

로그 문구 곳곳에 `은(는)` / `이(가)` / `을(를)` / `와(과)` / `(으)로`가 그대로 박혀 있었다
(차징 기술 준비 문구, 나무열매 섭취, 도발·앙코르 제약, 특성 교체·미라·떠도는영혼·모래뿜기,
변덕쟁이 랭크 변화, 스크린 소멸, 필드 변경 등). 받침 유무로 조사를 자동 판별하도록 교체.

- `BattleLogPage.tsx`: 기존 `eunNeun`/`iGa`/`eulReul`/`roEuro` 재사용 + **`waGwa`(와/과) 헬퍼 신설**.
  `CHARGE_TURN_MESSAGE` 맵 값에서 `은(는)` 제거하고 호출부에서 `eunNeun` 주입. 하드코딩 13곳 제거.
- `battleSimulator.ts`: 필드 변경 announce/log 2곳 `(으)로` → `roEuro`.
- `roEuro` **ㄹ 받침 보정**: `code%28===8`(ㄹ)도 `로`로 (예: `서울으로` → `서울로`). 두 파일 모두.
- 스코프 밖: 프리셋 삭제/불러오기 `window.confirm`의 `"${이름}"을(를)`(로그 아님) — 별도 요청 시.
- 검증: 조사 판별 순수 로직 스팟체크(팬텀→은/이/을/과/으로, 조로아크→는/가/와/로, 리플렉터가·
  빛의장막이·오로라베일이, 그래스필드로, 서울로) + `tsc`/`oxlint`/`build` 통과.
