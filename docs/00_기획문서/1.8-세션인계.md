# ver.1.8 세션 인계 (2026-09-26 기준)

컨테이너가 사라져도 새 세션에서 이어갈 수 있도록 남기는 문서. 새 세션은 **아래 "시작 프롬프트"를 그대로 붙여 넣으면** 된다.
작업이 진행되면 이 문서의 "현재 상태"를 갱신한다.

## 시작 프롬프트

```
poke-builder(Erensia/poke-builder) ver.1.8 AI 작업을 이어서 한다. 먼저 docs/00_기획문서/1.8-세션인계.md 를 읽고
"작업 규칙"을 따르며 "현재 상태"의 다음 작업부터 진행해줘. 답은 한국어로.
```

## 작업 규칙 (사용자 확정)

- 기획 문서보다 **코드가 우선** — 코드를 먼저 확인한다. 설계가 필요한 건 설계안을 먼저 제안하고 합의 후 구현.
- 항목마다 dev에서 브랜치 → PR을 dev로 **squash 머지**(PR 1개 = 커밋 1개). 머지한 브랜치는 삭제(원격 자동 삭제 켜짐).
  dev → main은 **ver.1.8 끝에 merge commit**으로.
- 기획 문서만 바꾸는 변경은 dev에 직접 커밋. 기능 작업 중 발견한 버그는 그 기능 커밋에 포함.
- 머지 때 README 최신화. 커밋 메시지 끝에 Co-Authored-By / Claude-Session 줄.
- 커밋 전 `npm run lint` + tsc(`npx tsc -b`) + `npm run test:ai`.
- **엔진 변경**: `node scripts/battle-ai-sim.mjs regress 300` 해시를 전후 비교해 `battle-ai-decision-layer.md` §12에 기록.
  데이터·엔진 변경이 섞이면 "이전 데이터 + 새 코드" 해시를 따로.
- **AI 튜닝 판정**: 그리디 승률 + h2h. 벤치는 **5개 병렬**(줄이지 말 것 — 사용자 지시), 켬/끔 토글로 비교:
  `node scripts/battle-ai-sim.mjs greedy 800 [params]` ×2(켬/끔), `STATUS=1 … greedy 400` ×2, `STATUS=1 … h2h 200 '{켬}' '{끔}'`.
  벤치 도중 작업 트리를 바꿀 거면 `git worktree`로 벤치 전용 체크아웃을 만들어 돌린다(sim은 시작 시 코드를 한 번 읽음).
- 벤치가 도는 동안 다음 항목을 이전 브랜치 끝에서 따서 진행, 앞 PR이 squash 머지되면 `git rebase --onto origin/dev <이전 끝> <브랜치>`.
  다음 항목 사전 조사를 미리 해 두고, 결정할 점은 바로 보고.
- **브라우저 화면 검증은 사용자에게 맡긴다**(claude-in-chrome 직접 사용 금지). 부탁 전에 해당 작업 브랜치를 원격에 올려 두고
  사용자에게 PC에서 전환 방법을 안내. 테스트 중 localStorage `champions-party-sim.party.v1`(파티 자동저장)은 절대 지우지 말 것.
- 한국어로 답한다(사용자는 영어가 어렵다).

## 현재 상태

- dev 최신: 3선출 AI(#153) · 한계점 정리 ①(#154)·②·③·④ 머지 완료 — 결정 레이어 §14. ②③④ 적용 AI 그리디 1011/1600(이전 980),
  ④ 켬/끔 h2h 215:184. 턴당 판단 평균 107ms·최대 837ms(단독).
- **다음(내일 시작)**:
  1. **⑤ 동률 랭크업 확인** — 확정 처치가 아닌 공격(c ≈ 1.1)과 랭크업이 동률(±0.1)일 때 랭크업을 고르는 경우(§13 ③). diag로 빈도를
     보고, 필요하면 동률 처리에 "c < 1.5면 공격 우선" 같은 규칙을 켬/끔 벤치로 확인(작은 작업).
  2. **ver.1.8 마무리** — README·백로그 ver.1.8 완료 정리, 1.9 백로그 확정, **dev → main merge commit**(사용자 확인 후).
  3. 사용자 확인 대기: 플레이테스트 체크리스트 `docs/00_기획문서/배틀AI 기획/hard-ai-playtest-checklist.md`.
- 사용자 결정: `partyEffects`(전부)는 끔, 종류별 5종만 켬. 불완전 정보(쉬움 B안) 등은 1.9(`1.9-backlog.md`).
- 시뮬레이터: `"difficulty":"easy"`, h2h `OPP_ROOT`, ai `NAN_DUMP=1`, `select` 모드(`SELECT_B=random|first`).
- regress 300판 해시 `8fd139d9…`.

## 다음 작업 (백로그 §4 로드맵 순서, 사용자 확정)

1. ~~변화기 판단 커버리지(A1·A2·Tier 2)~~ 완료 · ~~트랙 M~~ 완료 · ~~트랙 L~~ 완료
2. ~~로드맵 3~~ 완료
3. ~~턴 종료 효과~~ 완료
4. ~~어려움 난이도 종합 테스트~~ 완료(플레이테스트는 사용자 확인 대기)
5. ~~쉬움 난이도~~ 완료(A안, 화면 확인 대기)
6. ~~3선출 AI~~ 완료
7. **단계별 AI 한계점 최종 다듬기**(①~④ 완료, ⑤ 남음)(사용자 결정 — 각 단계의 근사는 문서에만 남기고 여기서 한꺼번에): 도발·신비의부적 가치 보정,
   멸망 카운트 이월 근사, 페어리록 미반영, 문어굳히기 누적 근사, 첫 선택지 기본 동작(모든 선택지 −∞) 등
8. ver.1.8 끝: dev → main merge commit

## 참고 위치

- 백로그: `docs/00_기획문서/1.8-backlog.md` §4(로드맵)·§4-1(변화기 분류)·§5(트랙 M)
- AI 결정 레이어: `docs/00_기획문서/배틀AI 기획/battle-ai-decision-layer.md` §4-5(파티 평가)·§4-9(Tier 2)·§4-10(로드맵 3)·§9(파라미터)·§12(검증·해시)
- 데이터 변경 이력: `docs/데이터변경이력.md`
- 코드: 엔진 `src/lib/battle/`, AI `src/lib/battle/ai/`(evaluator·decision·partyEval·statusMoveEffects), 시나리오 테스트
  `scripts/battle-ai-scenarios.mjs`, 시뮬레이터 `scripts/battle-ai-sim.mjs`
